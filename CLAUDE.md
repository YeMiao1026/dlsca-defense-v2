# DLSCA 防禦端專案計畫書

**專案代號**：`dlsca-defense-v2`
**所屬專題**：基於生成對抗網路之主動式對抗旁通道防禦機制
**文件版本**：v0.1（規劃階段，尚未開始實作）
**與攻擊端的關係**：攻擊端 `dlsca-attack-v2` 是獨立 sibling repo，不共用程式碼、只共用資料格式介面（見 §2）。

---

## 1. 目標與核心論點

**核心論點**：用 GAN（或對抗訓練式擾動產生器）訓練出的防禦，在**同樣的擾動成本下**，比單純加高斯噪訊或時域干擾（desync-style jamming）能提供更好的防禦效率——用更少的成本，讓攻擊者需要更多軌跡才能破解金鑰。

這不是「GAN 防禦有沒有效」，是「GAN 防禦是否比簡單基準線**更有效率**」——實驗設計必須是**成本匹配的比較**，不是各自跑各自的、事後拿不同成本的結果比較。

## 2. 跟 dlsca-attack-v2 的介面

**不共用程式碼，只共用資料格式**——這是刻意的邊界，避免防禦端的實驗設計偷偷依賴攻擊端內部實作細節（也避免兩個 repo 的 git 歷史/版本相依糾纏在一起）。

防禦端要做的事：讀取已訓練好的攻擊者（`dlsca-attack-v2/runs/{run}/model.keras` + `config_snapshot.yaml` + `split_indices.npz`），輸出一個防禦後的 E 集軌跡陣列（`.npy`，shape 跟攻擊端 `attack_traces[split.e]` 完全一致），然後呼叫攻擊端既有的 Stage B 介面做評估：

```bash
# 防禦端只需要產出 defended_traces.npy，其餘全部交給攻擊端既有管線
python3 <dlsca-attack-v2>/scripts/02_run_attack.py \
  --run <dlsca-attack-v2>/runs/E01_baseline_clean_20260816_1302 \
  --traces defended_traces.npy --out results/probs.npy

python3 <dlsca-attack-v2>/scripts/03_evaluate.py \
  --run <dlsca-attack-v2>/runs/E01_baseline_clean_20260816_1302 \
  --probs results/probs.npy --out results/metrics.json
```

這保證防禦效果的判定（GE/N_TGE/PI，100次獨立重排攻擊）**跟攻擊端用的是同一套已經被 33 輪除錯驗證過的評估邏輯**，不會因為防禦端自己另外寫一套評估、寫出跟攻擊端不一致或不夠嚴謹的結果。

**成本指標**（`src/metrics/perturbation.py::psr/l2/linf`）是攻擊端已經實作好、防禦端直接複製過來用的純函式——不重新發明，見 §5。

## 3. 威脅模型（防禦端視角）

延續攻擊端 CLAUDE.md §5.1 的 profiled attack 威脅模型，防禦端這邊定義：

- **靜態攻擊者（A0，本階段目標）**：攻擊者已經訓練好一個固定模型（例如攻擊端的 E01），部署防禦前後都用同一個模型攻擊。這是最基本、最容易嚴謹評估的情境，也是本專案第一階段唯一目標。
- **自適應攻擊者（A1，後續延伸，不在本階段範圍）**：攻擊者發現防禦部署後重新訓練模型繞過。更有說服力但工程量大（需要防禦端先產出足量防禦後訓練資料，再讓攻擊端重跑一次完整訓練），留待第一階段結果出來後再決定是否投入。

**固定攻擊者的選擇是實驗設計的關鍵變數**（攻擊端 CLAUDE.md 附錄 C.2 已經證實：同一個防禦對「有沒有先天免疫力」的不同攻擊者效果差 4 倍以上）。本專案全程固定用 `dlsca-attack-v2` 的 **E01（clean baseline，未對任何防禦手法免疫過）**當基準攻擊者，不能換成 E02（噪訊增強訓練過，對高斯噪訊類防禦有內建免疫力，會系統性高估防禦效果）。

## 4. 舊版程式碼的已知問題（重新驗證前必須先修正）

`/mnt/c/Document/B11209017/ASCAD/GAN/train_improved_defender.py` 是上一輪專題已經做出來、有實測數據的對抗擾動產生器（非真GAN，無discriminator）。移植過來重新驗證前，先記錄兩個已知問題：

1. **訓練用的凍結攻擊者是 `cnnd_paper_model.h5`（`train_cnnd.py` 的產物）**——這正是攻擊端 CLAUDE.md 附錄 B.11 證實「幾乎沒學到東西」的那個模型（用嚴謹評估法重測，GE=199.27，比隨機基準還差）。**拿一個本身沒訓練起來的模型當對抗訓練的目標，防禦器學到的『讓攻擊者混淆』可能只是在利用這個模型的雜訊，不是真的學會了對抗一個有效攻擊者**。本專案的凍結攻擊者必須換成攻擊端已驗證有效的 E01（N_TGE=475）。
2. **舊版評估用的是攻擊端 `ASCAD_test_models.py::full_ranks()`**——單次、不重排、依原始儲存順序，正是攻擊端整個重構專案存在的理由（附錄 B.11 的「N_TGE≈100 是量測假象」那個坑）。舊版 `defender_summary.csv` 的「>1000條軌跡才破解」這個數字，可信度跟當年的「N_TGE≈100」是同一個等級，**不能直接引用，必須用攻擊端現在的100-run評估法重新量測**。

## 5. 專案結構

```
dlsca-defense-v2/
├── configs/
│   ├── base.yaml
│   ├── attacker/               # 指向 dlsca-attack-v2 已訓練攻擊者的路徑設定
│   │   └── e01_baseline.yaml   # attacker_run: <path-to-dlsca-attack-v2>/runs/E01_baseline_clean_...
│   └── exp/                    # D01, D02... 防禦實驗編號（比照攻擊端E01-E08慣例）
├── src/
│   ├── config.py                # 沿用攻擊端同一套 YAML 合併邏輯（複製，不 import）
│   ├── generator/
│   │   └── conv_perturber.py    # 移植 build_improved_defender()，見 §6
│   ├── train/
│   │   └── adversarial.py       # 對抗訓練迴圈，凍結攻擊者、只更新產生器
│   └── bridge/
│       └── attack_interface.py  # 讀 dlsca-attack-v2 的 model.keras/config_snapshot.yaml/split_indices.npz，
│                                 # 封裝成「餵 raw trace 進去、拿到 softmax 機率」的函式，供訓練迴圈算對抗loss用
├── scripts/
│   ├── 01_train_defender.py     # 對抗訓練，輸出 generator.keras
│   ├── 02_generate_defended.py  # generator 對 dlsca-attack-v2 的 E 集跑一次，輸出 defended_traces.npy
│   │                             # （之後接 dlsca-attack-v2 的 02/03，見 §2）
│   └── 03_compare_defenses.py   # 讀多個 metrics.json + cost_metrics.json，畫 PSR-vs-GE 比較圖
├── src/metrics/
│   └── perturbation.py          # 從 dlsca-attack-v2 複製 psr/l2/linf（同介面，同測試），保持成本指標一致
├── runs/                        # 訓練產物，git-ignored
├── results/                     # 防禦評估產物（呼叫攻擊端Stage B後的probs.npy/metrics.json落地處），git-ignored
├── tests/
├── docs/
│   └── runs.md                  # 比照攻擊端慣例
├── requirements.txt
└── README.md
```

## 6. 產生器架構（第一版，移植自舊專題）

延續 §4 的修正，架構本身先原封不動移植（已知能訓練，問題出在訓練目標不是產生器架構）：

```
Input(700,1)
  → Conv1D(32,k7,relu) → BN
  → Conv1D(32,k7,relu) → BN → Add(殘差)
  → Conv1D(16,k5,relu) → BN
  → Conv1D(8,k5,relu)  → BN
  → Conv1D(1,k3,tanh)
  → Lambda(x * epsilon)                    # 有界擾動 [-epsilon, epsilon]
```

**輸入輸出的前處理紀律**（新增，舊版沒處理）：產生器對**原始 raw trace**（跟攻擊端 `attack_traces`/`profiling_traces` 同一個 int8 尺度，cast float32）輸出擾動，`defended = raw + perturbation`，這個 raw 尺度的 `defended` 就是要存成 `.npy` 交給攻擊端 `02_run_attack.py --traces` 的東西——攻擊端會自己用它既有的 Standardizer/MinMax 流程把它轉換成攻擊模型吃的尺度，防禦端不用管這一段，也不該自己另外標準化（那樣輸出的 `.npy` 尺度會跟攻擊端期待的 raw 尺度對不上）。

**對抗訓練時**：為了算 `attacker(x_defended)` 的 loss，防禦端的訓練迴圈裡需要複製一份跟攻擊端一致的 Standardizer/MinMax（`src/bridge/attack_interface.py` 負責讀取攻擊端 `config_snapshot.yaml` 裡的 `preprocess` 設定、在 A 集上重新 fit，邏輯跟攻擊端 `02_run_attack.py` 重新 fit 的方式一致）——訓練時的 forward pass 是 `raw → +perturbation → standardize → minmax → frozen_attacker → loss`，跟部署/評估時最終會發生的事情完全一致。

**Loss（第一版，原封不動移植)**：`confusion(→均勻分佈的交叉熵) + λ_L2·||perturbation||² + λ_smooth·相鄰點平方差 + λ_entropy·|熵-log(256)|`，四個權重維持舊版的量級當起點（`λ_confuse=1.0, λ_L2=0.005, λ_smooth=0.002, λ_entropy=0.1`），重新驗證後再決定要不要調。

## 7. 成本指標與比較基準

`src/metrics/perturbation.py` 複製攻擊端同一份實作（`psr`/`l2`/`linf`，逐軌跡陣列輸出，不是純量）。跟攻擊端「高斯噪訊 PSR-vs-N_TGE」基準曲線（附錄 C.3，8個sigma_ratio點，PSR 0.0089–0.2674）比較時，**兩邊的 PSR 都要用同一個定義、對同一批 clean E 集算**，才能公平疊在同一張圖上。

## 8. 實驗編號規劃（草案）

| 編號 | 內容 | 依賴 |
|---|---|---|
| D01 | 對抗擾動產生器 vs E01（凍結攻擊者），重新驗證舊專題的假設 | §4/§6 修正過的版本 |
| D02 | D01 的 loss權重掃描，找出效率最好的配方 | D01 跑通 |
| D03 | **改編號**——loss權重掃描（D02）推翻了「smooth/l2拖累效率」假設後，改測「確定性擾動 vs 高斯噪訊獨立隨機性」這個結構性假設，見附錄A.5/D03 | D02 跑通 |
| D04 | **改編號**——SNR診斷（附錄A.8）找出真正機制（洩漏點覆蓋不完整）後，重新設計loss加入可微分洩漏抑制項，見附錄A.9/D04 | D01-D03 + SNR診斷完成 |
| D05 | **改編號**——根據B11209017/方向.md重新規劃的21天路線圖：通用擾動模板（universal perturbation，Gu式），支撐claim②「工程成本比masking低」+ Route A的第三種防禦類別對照組，見附錄A.14/D05 | D01-D04 + 方向.md路線圖確立後 |
| D06（原D05/D04/D03） | 自適應攻擊者（A1）情境：防禦部署後攻擊端重訓練——21天路線圖的核心項目，見附錄A.9下一步 | D05跑完 |
| D07（原D06/D05/D04） | 跟高斯噪訊基準曲線做 PSR 成本匹配比較（三方比較圖） | D01-D06 找到至少一個能打平/贏過高斯基準線的配方後再進行 |
| D08（原D07/D06/D05） | 時域干擾基準線（desync-style jamming，用攻擊端已有的 ASCAD_desync50/100 概念另外建一版） | 獨立於D01-D07，可平行做，21天內明確不排 |

## 9. 驗收標準（草案）

- [ ] D01 用嚴謹100-run評估法重新驗證舊專題假設，得出誠實數字（不管是否比舊版好）
- [ ] 至少一組 GAN 防禦配方，在**同樣PSR成本**下，GE@9000/N_TGE 比高斯噪訊基準曲線同一個PSR點更好
- [ ] 三方比較圖（噪訊 vs 干擾 vs GAN）可以直接放進期末報告
- [ ] 防禦訓練/評估流程可重現（config快照、種子、env.json，比照攻擊端紀律）

## 10. 已知風險

| 風險 | 對策 |
|---|---|
| 舊版凍結攻擊者不可靠（§4） | 換成攻擊端 E01，第一步就做 |
| 防禦端自己另外寫評估邏輯，跟攻擊端不一致 | 強制走 Stage B 介面，不自己算 GE |
| PSR 定義兩邊沒對齊 | 直接複製攻擊端 `perturbation.py`，不重新實作 |
| 靜態攻擊者情境下的「勝利」對自適應攻擊者沒說服力 | 明確標註 D01-D04 是A0情境，A1留作後續延伸，不要混著講 |

---

## 附錄 A：D01 執行結果

### A.1 產生器/攻擊者橋接（§4/§6 移植）已驗證

`src/bridge/attack_interface.py` 讀取 dlsca-attack-v2 的 `config_snapshot.yaml`＋`split_indices.npz`，在同一個 A 集上重新 fit Standardizer/MinMaxScaler，並把 transform 重新表達成 TF 常數運算，讓對抗訓練迴圈可以對「凍結攻擊者的 loss 對擾動」求梯度。`tests/test_bridge.py` 用合成資料驗證這個 TF 版本跟攻擊端 numpy 版本數值一致（這正是攻擊端 CLAUDE.md B.17/B.19/B.33 反覆踩過的「表面上做對了、實際上沒對齊」那類 bug 的預防性測試）。`BoundedPerturbation` 取代舊版的 `Lambda`，讓 `generator.keras` 可以正常 `save`/`load_model` 而不需要 `enable_unsafe_deserialization()`。

小規模（200條軌跡、2 epoch）在真實 E01 攻擊者上跑過整條鏈路（訓練→產生防禦波形→餵進攻擊端 `02_run_attack.py`/`03_evaluate.py`），確認接線正確、`metrics.json` 正常產出。

### A.2 D01 正式跑（GPU server，15000條軌跡，架構/loss權重原封不動沿用舊版）

`runs/D01_replicate_baseline_20260816_232552_948723/`。訓練在 epoch 34 觸發 patience=5 早停（loss 從 epoch 1 的 7.62 降到穩定在 5.605-5.607，confuse 項貼著 log(256)=5.545 的均勻分佈下限附近但沒有真正壓到底，l2/smooth 兩項持續下降代表擾動幅度在收斂過程中越變越小）。

對 E01 的 E 集（10000條軌跡）套用訓練好的產生器，成本：

```
PSR: mean=0.0249  median=0.0249  p90=0.0255  max=0.0265（各軌跡幾乎一致，變異極小）
L2:  mean=18.58
Linf: mean=2.38（遠低於 epsilon=6.0 上限，代表產生器並沒有把擾動預算用滿）
```

用攻擊端 Stage B 介面（`02_run_attack.py --traces/--out` + `03_evaluate.py`，100次獨立重排、`max_traces=9000`，跟附錄 C.3 高斯基準曲線同一套評估法）：

```
N_TGE  = None（未收斂）
N_SR90 = None
GE @ N=9000  = 111.84（100次獨立攻擊，全程在110-123之間緩慢震盪，沒有隨N拉寬持續下降的收斂跡象）
SR1 @ N=9000 = 0.0000
PI           = -0.0945（輕微負值，不是"confidently wrong"的崩潰量級）
```

### A.3 誠實結論：這一版跟同成本的高斯噪訊基準線比，效率明顯更差

對照攻擊端附錄 C.3 的高斯噪訊 PSR 掃描（同樣固定 E01 為攻擊者、同一套 100-run 評估法）：

| 防禦手法 | PSR | N_TGE | GE@9000 |
|---|---|---|---|
| 高斯噪訊 sigma_ratio=0.1 | 0.0089 | 514 | 0.00（完全收斂） |
| 高斯噪訊 sigma_ratio=0.25 | 0.0223 | 775 | 0.00（完全收斂） |
| **D01 GAN 產生器（本次）** | **0.0249** | **None** | **111.84（幾乎沒有真正壓垮攻擊者）** |
| 高斯噪訊 sigma_ratio=0.5 | 0.0446 | 2276 | 0.00（完全收斂） |

**在幾乎相同（甚至略高）的 PSR 成本下，單純的高斯噪訊讓攻擊者完全瓦解（GE精確降到0、100次攻擊全部收斂），這次移植的 GAN 產生器卻只把 GE 從隨機基準127.5壓到111.84，離攻擊者真正被瓦解還差得遠。** 這跟這個專案最初想證明的方向（GAN 防禦應該比噪訊更有效率）正好相反——這是舊版 `train_improved_defender.py` 架構與 loss 權重原封不動移植後，換上真正有效的攻擊者、換上嚴謹評估法之後得到的誠實結果，不是移植過程有 bug（§4 提到的兩個已知問題都已修正，且整條鏈路有測試與端到端smoke test驗證）。

**初步機制解讀（尚未驗證，留待後續投入判斷是否深究）**：高斯噪訊是逐點獨立同分佈的隨機噪訊，直接推高每個時間點的類內變異數，等同直接打壓 SNR 的分母；D01 訓練出的擾動 PSR 雖然數值相近，但由 `train_history.csv` 看得出來 l2/smooth 兩項在訓練過程中持續被壓低（代表 loss 權重把產生器推向「幅度小、相鄰點平滑」的解），而且 PSR 在10000條軌跡間幾乎不變（變異度極小，見上表 mean≈median≈p90）——這意味著產生器學出來的擾動對所有軌跡而言可能高度相似（低軌跡間變異），比較像是對洩漏點加了一個接近固定的偏移量，而不是像高斯噪訊那樣對每條軌跡注入獨立隨機性。一個固定偏移量會被 Standardizer/MinMaxScaler（在乾淨 A 集上 fit、對所有 E 軌跡套用同一組逐點統計量）部分「內部消化」掉——如果攻擊者本來就是靠逐點統計量在做判別，一個系統性但軌跡間一致的偏移，對「拉開類間變異、增加類內變異」這個判別任務的破壓效果，天生就會比同成本的隨機噪訊弱。**這是否為真正機制、以及loss權重（尤其是lambda_smooth）是否是主因，需要 D02 的超參數掃描才能確認，這裡先誠實記錄現象與初步假設，不下定論。**

### A.4 D02：loss 權重掃描，四點全部驗證 A.3 假設不成立

四個設定，架構/epsilon/confuse/entropy權重與n_train/epochs上限全部沿用D01，只動`lambda_l2`/`lambda_smooth`（皆用攻擊端Stage B介面、100-run、max_traces=9000評估，跟D01/附錄C.3同一套方法）：

| 設定 | lambda_l2 | lambda_smooth | PSR mean | GE@9000 | PI |
|---|---|---|---|---|---|
| **D01（基準）** | 0.005 | 0.002 | **0.0249** | **111.84** | -0.0945 |
| D02_nosmooth | 0.005 | 0.0 | 0.0393 | 137.32 | -0.0959 |
| D02_lowreg | 0.0005 | 0.0002 | 0.0636 | 194.83 | -0.0974 |
| D02_nol2 | 0.0 | 0.002 | 0.0974 | 187.70 | -0.0969 |
| D02_noreg | 0.0 | 0.0 | 0.1493 | 197.76 | -0.0974 |

**四點結果完全一致、方向跟A.3的假設相反**：拿掉/降低正則化後，PSR成本全部上升（最多到D01的6倍），但GE不但沒有變好（更低），反而全部變差（更高，多數還輸給隨機基準127.5）——D01自己的（滿正則化）配方，是這五個裡面唯一一個把GE壓到隨機基準以下的。換句話說：**smooth/l2懲罰不是在拖累防禦效果，拿掉它們讓訓練變得明顯更差**，即使花更多成本也一樣。A.3提出的「D01效率不如高斯噪訊是因為smooth/l2把擾動壓成太平滑太一致，被攻擊者的逐點統計量吸收」這個假設，被D02四個點正面推翻。

**修正後的解讀**：訓練log顯示所有五個設定的confuse loss項最終都收斂到幾乎同一個值（≈5.598，非常接近log(256)=5.545的均勻分佈下限），代表**不管有沒有正則化，產生器都學會了把單一軌跡的預測分佈推向差不多同樣程度的「均勻化」**——這解釋了為什麼五個設定的PI也全部緊貼在-0.0945到-0.0974這個窄範圍內，不管GE差多少（111.84到197.76）：PI量的是單軌跡層級的confusion程度（全部設定接近），GE量的是這個confusion在幾千條軌跡的log-likelihood累加後，是否還殘留可被攻擊者利用的、跟正確金鑰相關的系統性訊號（差異巨大）。**沒有正則化的版本，confusion本身沒有更強，但拿掉約束後產生器學到的擾動形狀，反而讓這個殘留訊號更容易被多軌跡累加找出來**——正則化在這裡看起來不是「阻力」，而是幫助訓練找到一個不會留下這種可累加殘留訊號的解。

這也把附錄A.3裡「擾動被攻擊者的逐點Standardizer/MinMax部分吸收」這個機制猜測，重新導向一個更根本的結構性解釋：這整個產生器家族（不論正則化強弱）都是**輸入軌跡的確定性函數，同一條軌跡每次都會得到同一個擾動**，不像高斯噪訊那樣對每條軌跡獨立注入隨機性——確定性擾動的某個成分本質上可能是「可被攻擊者的固定判別函數部分預測/抵銷」的，而獨立隨機噪訊天生做不到這件事。D01 vs 高斯基準線的效率差距，可能主要來自「確定性 vs 隨機性」這個結構性差異，而不是loss權重這個維度——這點loss權重掃描本身無法回答（五個設定都是確定性函數），需要另外設計實驗（例如在產生器輸出後疊加一層可控的隨機成分，或比較「同一模型多次inference是否給出相同擾動」跟「打亂/重跑seed是否改變攻擊者-可利用性」）才能驗證。

### A.6 D03：確定性 vs 隨機性假說，三點同樣被推翻

`src/generator/conv_perturber.py::AlwaysOnStochasticNoise`——跟Keras內建的`GaussianNoise`不同，這個層不管`training`旗標為何，每次呼叫都真的採樣新的雜訊並疊加到tanh-bounded delta上（再clip回[-1,1]），確保同一條軌跡在訓練或推論時每次呼叫都得到不同擾動，不是輸入的純函數。以D01（唯一勝過隨機基準的滿正則化配方）為基礎，疊加三個`noise_std`：

| 設定 | noise_std | PSR mean | GE@9000 | PI |
|---|---|---|---|---|
| **D01（基準，noise_std=0）** | 0 | **0.0249** | **111.84** | -0.0945 |
| D03_noise_low | 0.05 | 0.0341 | 119.54 | -0.0960 |
| D03_noise_mid | 0.15 | 0.0610 | 138.30 | -0.0995 |
| D03_noise_high | 0.30 | 0.1072 | 137.14 | -0.1034 |

**又是同一個模式**：noise_std越大，PSR成本越高，但GE也越差（0.05時119.54還壓在隨機基準127.5以下，0.15/0.30已經超過127.5、比什麼都不做還差）。**「確定性擾動缺乏隨機性」這個附錄A.5提出的假說，被這三點正面推翻**——加入獨立隨機性不但沒有幫助，反而讓效果單調變差，跟D02拿掉正則化的結果是同一種模式。

### A.7 兩個假說接連被推翻後的新假說：這可能是「對抗樣本」而非「資訊破壞」的問題

D02（四點）+ D03（三點），總共七個偏離D01設定的變體，**方向各不相同（放鬆正則化 vs 注入隨機性），但結果完全一致**：任何偏離D01那個精確、緊約束、平滑、確定性的解，PSR成本都上升，GE也都變差。這個一致性本身就是一個線索——問題可能不在「這個loss權重」或「有沒有隨機性」這類表層旋鈕，而在**這整個訓練目標的本質**。

`train_improved_defender.py`（及本專案原封不動移植的版本）的confuse loss，是標準的**對抗樣本（adversarial example）攻擊手法**：對一個凍結分類器，用梯度下降找出能讓輸出偏離正確標籤的最小擾動。這類方法的已知特性（ML安全文獻的常見觀察）是：梯度找到的擾動通常是**針對這一個特定模型的決策邊界的局部、脆弱捷徑**，而不是真的破壞輸入裡的底層資訊——換句話說，D01訓練出來的擾動可能只是學會了「怎麼讓E01這個特定攻擊者在單一次query上分類錯誤」，但底層的洩漏資訊（SNR意義上的訊號）可能大部分還在，只是被巧妙地移到了決策邊界的另一側。攻擊端的100-run log-likelihood累加評估法，正是設計來戳破這種「單次query被騙、但資訊還在、累加多次query後又能反推出來」的假象——這跟附錄A.3觀察到的「PI（單軌跡confusion程度）幾乎不變、GE（多軌跡累加後的可利用性）卻天差地遠」完全吻合。相對地，高斯噪訊完全不管任何模型的決策邊界，暴力地對每個時間點注入獨立雜訊，直接推高類內變異數、壓低SNR分母——這是在破壞底層資訊本身，不是在鑽某個模型的決策邊界漏洞，所以不管換哪個攻擊者、累加多少次query，效果都穩定。

**這個假說是可以直接驗證的，且工具已經現成**：攻擊端`src/metrics/leakage.py::snr`/`nicv`可以直接對「乾淨E集」vs「D01防禦後的E集」算逐點SNR，跟同PSR的高斯噪訊對SNR的影響做對照。如果D01的擾動幾乎不影響SNR峰值（訊號還在），而高斯噪訊在同PSR下明顯壓低SNR峰值，就直接證實這個假說；如果D01的擾動同樣有效壓低SNR，代表問題出在別的地方，需要重新想。

### A.8 SNR驗證：A.7的簡單版本被推翻，但揪出了更精確的真正機制——洩漏點覆蓋不完整

新增 `dlsca-defense-v2/scripts/03_check_snr_reduction.py`：不需要重新訓練，直接讀已有的`defended_traces.npy`（GAN跟高斯，同PSR成本），用攻擊端`src/metrics/leakage.py::snr`（已有、已測試過的純函式）比較「防禦前後」的逐點SNR下降幅度。

**第一次嘗試（ID標籤，即E01實際訓練的目標）：方法本身有問題，作廢**——乾淨E集在ID（未遮罩）標籤上的SNR峰值只有0.0335，本身就已經在雜訊層級（ASCAD遮罩機制設計上就是要讓一階洩漏在未遮罩標籤上趨近於0，這也是為什麼E01非得靠CNN學高階/多變量組合才能破解——見攻擊端`dlsca-attack-v2` CLAUDE.md附錄B.11對masked/unmasked標籤差異的討論）。高斯噪訊防禦後這個值甚至還「上升」了2.9%，這種雜訊層級的波動不具任何意義。**用univariate ID-SNR測A.7，metric本身選錯了，第一次結果不採信。**

**改用 ID_MASKED（mask_index=0，遮罩已知標籤，physically真實存在的洩漏點）重新測，在遮罩值本身的洩漏點（point 517）**：

```
乾淨E集 峰值SNR: 6.6787 @ point 517
GAN防禦後:        4.4535 @ point 514（-33.3%，PSR=0.0249）
高斯防禦後:       4.5303 @ point 517（-32.2%，PSR=0.0223，成本還比GAN低）
```

**GAN在這個點上壓低SNR的幅度，跟高斯噪訊幾乎一樣（甚至略多），成本還略高一點點——A.7原本「GAN沒有真的壓低SNR」這個簡單版本的假說，在這個洩漏點上被推翻。**

**但檢查第二個必要成分——遮罩值r_out本身的洩漏點（point 156，跟plaintext/key無關，純粹測r_out是否直接洩漏在某個時間點）——差異就出現了**：

```
乾淨E集 峰值SNR: 1.3316 @ point 156
GAN防禦後:        1.2751 @ point 182（-4.2%，峰值位置還偏移了，代表GAN根本沒有針對這個點）
高斯防禦後:       1.1790 @ point 156（-11.5%，峰值位置沒變，代表這裡被均勻地壓低了）
```

**高斯噪訊對這個點的壓制幅度是GAN的2.7倍**，且高斯的峰值位置完全沒有偏移（因為它是逐點獨立、無差別地對整條波形加噪，天生就會同時打到所有洩漏點），而GAN的峰值位置從156偏移到182，代表GAN的擾動根本沒有集中在這個真正的洩漏點上。

**修正後的機制解讀（比A.7原本的說法更精確、更有證據支撐）**：ASCAD這類遮罩防禦需要攻擊者同時掌握**兩個**東西才能破解——遮罩後的值（point 517）跟遮罩本身的值（point 156），E01光靠ID標籤訓練就能破解，代表它學到的是某種結合這兩個點的高階/多變量組合。**GAN訓練出來的擾動，把有限的擾動預算幾乎全部押在point 517上（在那裡表現跟高斯打平甚至略好），卻幾乎沒有動到point 156**——這很可能是因為訓練用的confuse loss只在乎「讓凍結攻擊者E01這一個特定模型在單次query上出錯」，梯度下降找到的捷徑剛好集中在對E01決策邊界最敏感的那個點，沒有動機去顧及point 156這個「攻擊者理論上也需要、但可能不是梯度最敏感方向」的第二洩漏點。高斯噪訊沒有這種「挑最敏感方向」的偏好，逐點獨立无差別加噪，天生會同時覆蓋兩個洩漏點——這正是D02、D03七次系統性嘗試（不管怎麼調loss權重或加隨機性，都沒能讓GAN打贏）背後真正的結構性原因：**問題不在「夠不夠隨機」或「夠不夠平滑」，而在「梯度下降找到的攻擊捷徑，天生傾向於只覆蓋部分洩漏點，而不是像高斯噪訊那樣無差別覆蓋全部洩漏點」**。

### A.9 D04設計：可微分的洩漏抑制loss（`src/metrics/leakage_loss.py`）

**設計目標**：把loss從「只讓E01單次query出錯」換成「同時壓低所有已知洩漏點的可分性」，直接對應A.8診斷出的機制。

**關鍵設計決策——為什麼用CPA式相關係數，不用真正的批次內class-conditional SNR**：`src/metrics/leakage.py::snr`需要對256個類別分別算類內變異數，batch_size=64的話大多數類別在一個batch裡只有0-1個樣本，統計量不穩定、不能用。改用**平方Pearson相關係數**（defended trace逐點跟label的Hamming weight代理之間）——這是CPA（Correlation Power Analysis）的標準做法，物理假設是「功耗與處理資料的Hamming weight近似線性相關」，這個假設讓CPA能用遠少於SNR/NICV所需的樣本數就估出穩定的相關係數，是SCA領域行之有年的權衡（線性模型假設換取小樣本可用性；如果真實洩漏不是線性於HW，這個loss項會跟CPA本身一樣有盲點——這點需要明確記錄）。

**Soft-max（logsumexp）而非硬max或mean**：對整條軌跡700個點的平方相關係數取硬max只會在當前最強的那一個點給梯度（容易「打地鼠」，這一步壓下去、換一個點冒出來，訓練不穩定）；取mean會被699個不相關的點稀釋掉真正重要的1-2個點的梯度訊號。Soft-max用temperature參數在兩者之間取得平衡，同時對「目前偏高的所有點」都給出有意義的梯度，且處處可微。

**新增檔案**：`src/metrics/leakage_loss.py`（`hamming_weight`/`pointwise_squared_correlation`/`soft_peak_leakage`）、`src/leakage_probe.py`（從metadata算出兩個洩漏頻道的label：`masked_value`＝Sbox[p⊕k]⊕mask，`mask_value`＝mask本身；`AES_SBOX`從攻擊端複製過來，不跨repo import，同`perturbation.py`的既有原則）。`src/train/adversarial.py::fit()`新增`leakage_labels`/`lambda_leakage`/`leakage_temperature`三個參數，**`lambda_leakage=0.0`（預設）完全不計算這個loss項，不是零權重加總**——確保D01/D02/D03既有config的行為逐位元不變。`configs/base.yaml`新增對應預設值（含`leakage_probe.enabled: false`開關）。6個新測試（`test_leakage_loss.py`）+ 1個新測試（`test_leakage_probe.py`）+ 2個`test_adversarial.py`新測試，`tests/`現在36個測試全過。

**設計過程中發現的一個重要理論限制（寫測試時才浮現，值得記錄）**：第一版驗證測試原本想確認「對一個有洩漏的欄位直接做梯度下降，能不能讓這個loss下降」，用一個**可訓練的仿射擾動**（純量縮放/平移，不依賴輸入）去優化，結果loss完全不動——诊断後發現這是**數學上必然的**：Pearson相關係數對「被觀察變數的任何非零仿射變換」不變（`corr(a·x+b, y) = sign(a)·corr(x,y)`，跟`a`、`b`的大小完全無關，只有正負號可能翻轉）。這代表：**純粹縮放或平移一個洩漏欄位，不管縮放係數多小，都不會讓這個loss下降**（只有在縮放係數趨近於0、也就是幾乎把整個欄位歸零時，才會因為分母的極小值保護項而讓相關係數退化到0）。测试改成讓可訓練變數控制**注入的獨立雜訊量**（不是縮放/平移，是真正加入額外變異）後，loss如預期正確下降——**這證實了這個loss真正獎勵的是「注入獨立雜訊」這類手法，如果只是仿射變換洩漏欄位是無效的**。這對D04的產生器設計有直接意義：D01的產生器目前是純確定性函數（`noise_std=0`），如果它學到的解只是對洩漏欄位做簡單縮放/平移，這個新loss不會有效果；CNN要嘛得學會利用其他時間點的資訊建構出一個「相對於label是隨機的」訊號（架構上可行，因為Conv1D對整條700點軌跡都有感受野），要嘛就需要搭配D03已經做好的`AlwaysOnStochasticNoise`（`generator.noise_std>0`）提供真正的隨機性來源——這是第一次D04實驗設計時特意先只測純確定性版本（`configs/exp/D04_snr_aware.yaml`保持`noise_std=0`）的原因：先確認CNN的跨欄位資訊利用能力夠不夠，不夠的話再加隨機性，兩個變數不要一次疊加，才知道效果歸功於誰。

**已完成端到端smoke test**（小規模：300條軌跡、2 epoch，對真實E01攻擊者）：確認`leakage_probe.enabled=true`能正確從metadata算出兩個洩漏頻道的label，訓練迴圈正確把`leak_masked_value`/`leak_mask_value`兩項算進total loss並記錄進`train_history.csv`，管線接線正確。**尚未跑正式的15000條軌跡/40epoch GPU實驗**——`configs/exp/D04_snr_aware.yaml`已經寫好（`lambda_leakage=0.05`，其餘沿用D01），下一步是實際跑一次並用攻擊端Stage B介面做100-run正式評估。

### A.10 下一步（留待後續決定）

- **正式跑D04**：`configs/exp/D04_snr_aware.yaml`（GPU，15000條/40epoch），完成後用攻擊端Stage B介面正式評估、跟D01（GE@9000=111.84）及同PSR高斯基準線比較。
- 如果D04的GE明顯優於D01但還是打不贏高斯基準線，下一步是重新跑一次`--override generator.noise_std=0.05`（或類似）疊加D03已驗證過的隨機性層，驗證上一段提到的「CNN跨欄位能力不夠、需要真隨機性搭配」這個備案假設。
- 如果D04本身就找到一個能打平/贏過高斯基準線的PSR點，才進入D05（原D04，三方比較圖）。

### A.11 TVLA檢查——方向.md的預測完全命中，「爆表」不是比喻

根據 `B11209017/方向.md` claim①的建議，新增 `scripts/04_check_tvla.py`：對兩個已知洩漏頻道（`masked_value`、`mask_value`，跟SNR診斷附錄A.8同一組）逐bit做Welch t-test（`dlsca-attack-v2/src/metrics/leakage.py::t_test`，跟SNR診斷同一種cross-repo引用方式），掃過8個bit取最壞（|t|最大）的那個，對照TVLA慣例門檻`|t|>=4.5`。

**ASCAD的Attack_traces沒有重複相同明文的「fixed」採集組**，所以做不了教科書等級的fixed-vs-random明文TVLA；改用SCA領域常見的替代做法——「specific」TVLA：用已知中間值的某個bit把軌跡切成兩組再做t檢定，等於單bit版的DPA/leakage detection，跟SNR/NICV同樣是model-agnostic（不涉及任何訓練模型）。

**結果**（D01 vs 高斯σ=0.25，PSR≈0.023-0.025同一組防禦後波形）：

```
channel: masked_value（point 517附近）
  clean（正對照組）    worst |t| = 71.73   -> LEAKAGE DETECTED
  GAN-defended         worst |t| = 67.61   -> LEAKAGE DETECTED
  Gaussian-defended    worst |t| = 68.80   -> LEAKAGE DETECTED

channel: mask_value（point 149附近）
  clean（正對照組）    worst |t| = 52.82   -> LEAKAGE DETECTED
  GAN-defended         worst |t| = 52.98   -> LEAKAGE DETECTED（比clean還高！）
  Gaussian-defended    worst |t| = 50.70   -> LEAKAGE DETECTED
```

**跟方向.md的預測幾乎逐字命中**：「這個實驗很可能給你們負面結果...洩漏根本沒減少，只是位置被搬走了...TVLA大概率仍然爆表」——確實爆表，而且不是差一點點：門檻是4.5，兩個防禦、兩個頻道量出來的|t|全部落在50-72之間，是門檻的11到16倍，GAN跟Gaussian從clean的71.73/52.82只降到67-69/51-53，降幅在5-10%之間，遠遠不足以讓|t|掉到門檻以下。**mask_value頻道上GAN甚至比clean的|t|還高**，等於這個bit的可分性完全沒有被觸碰。

**這個結果比SNR診斷（附錄A.8）更嚴厲，也補上了一塊SNR沒講清楚的東西**：SNR診斷看的是「峰值降了多少百分比」（GAN在masked_value降了33%，看起來像是有實質壓制），但TVLA用的是「絕對可分性夠不夠低到偵測不到」這個security-evaluation的標準二元判準——即使SNR峰值降了三成，剩下的七成在TVLA的尺度下仍然是壓倒性的洩漏，跟「有沒有真的防住」完全是兩回事。**這正是方向.md claim①一開始就講的不對稱：安全性主張只能被證偽，做SNR/PI這類連續指標容易讓人誤以為「有改善=有防護」，TVLA用的是攻防評估慣用的二元門檻，比較不會製造這種錯覺。**

**這對整個防禦敘事的意義**：不只是「GAN輸給高斯」，是「在我們目前測過的PSR成本範圍內，GAN跟高斯這兩種手法都沒有真正達到TVLA意義下的安全——它們都只是讓某一個特定攻擊模型的single-query分類變困難，底層的model-agnostic洩漏幾乎原封不動」。這比D01-D04單純比較「誰的GE比較低」更根本，也更貼近方向.md推薦的Route A論述核心（「失效是資訊理論的，不是工程的」）——現在有TVLA這個獨立、model-agnostic的證據支撐這句話，不只是靠SNR的間接推論。

**尚未做、留待後續**：目前只測了一個PSR點（高斯σ=0.25），還沒掃過附錄C.3整條高斯曲線去看TVLA會不會在更高成本（例如σ=1.5，那個讓GE完全不收斂的門檻點）真的通過門檻——如果連σ=1.5都還是爆表，代表「用等功率噪訊/擾動達到TVLA意義下的安全」在這個資料集上的成本可能高到不切實際，這本身會是一個值得寫進報告的量化結論。

### A.12 TVLA掃過整條高斯曲線：連PSR最高的點都還差3.5-4.5倍，外插估計要幾百倍σ才夠

把A.11同一套bit-split TVLA對附錄C.3全部8個高斯sigma_ratio點（0.1/0.25/0.5/0.75/1.0/1.5/2.0/3.0）各跑一次（GAN欄位固定用D01當參照，Gaussian欄位隨sigma變化）：

| sigma_ratio | PSR | masked_value worst\|t\| | mask_value worst\|t\| |
|---|---|---|---|
| 0.1 | 0.0089 | 71.27 | 52.32 |
| 0.25 | 0.0223 | 68.80 | 50.70 |
| 0.5 | 0.0446 | 61.66 | 46.35 |
| 0.75 | 0.0668 | 53.58 | 40.96 |
| 1.0 | 0.0891 | 46.33 | 35.85 |
| 1.5 | 0.1337 | 35.51 | 27.85 |
| 2.0 | 0.1783 | 28.45 | 22.46 |
| **3.0（C.3測過的最高成本點）** | **0.2674** | **20.28** | **16.11** |

**連PSR最高、噪訊振幅接近軌跡本身振幅27%的3.0點，兩個頻道的|t|都還是門檻4.5的3.5-4.5倍，遠遠沒有通過**。且|t|隨sigma的下降速度很慢——sigma從0.1拉到3.0（30倍），masked_value的|t|只掉了3.5倍（71.27→20.28），不是等比例甚至更快下降。用全部8點做log-log冪律回歸外插（`|t| = C·σ^a`）：

```
masked_value：斜率 a=-0.364，外插 |t|=4.5 所需 sigma_ratio ≈ 396
mask_value  ：斜率 a=-0.338，外插 |t|=4.5 所需 sigma_ratio ≈ 294
```

**外插到sigma_ratio≈300-400，遠遠超出實測範圍（0.1-3.0），這個數字的精確度不能太當真**——冪律關係不保證能外插兩個數量級以上，噪訊主導區間的統計行為可能完全不同。但即使只當一個量級估計：sigma_ratio=300代表噪訊振幅是軌跡本身逐點標準差的300倍，這已經不是「加一點噪訊」的等級，是把原始訊號整個淹沒到不知道還剩不剩軌跡本身意義的程度——**用簡單的i.i.d.高斯噪訊要達到TVLA意義下的安全，在這個資料集上的成本高到不切實際，這是一個穩健、不太需要外插就能下的結論**（8個實測點裡最貴的那個都還差3.5-4.5倍，這個差距本身已經夠說明問題，外插只是把「差多少」量化得更具體）。

**這個結論直接支撐Route A論述、也呼應方向.md「唯一可證明的路」那段**：真正的masking之所以能講「可證明」的安全，是因為它有d-probing/noisy leakage這種形式化模型，用可控的方式注入「夠多」的隨機性；而我們這裡實測到的是——**在沒有形式化模型指導、單純「加噪訊」的路線下，光是憑經驗掃描就能看出成本會爆炸式增長，不需要等到真的去推導理論界，光是這條實測曲線的形狀就已經足夠當作「為什麼需要真正的masking而不是hiding-only防禦」的量化證據**。這比單純講「TVLA爆表」更進一步——不只是「現在沒過」，是「用這個手法要過的代價高到不合理」。

### A.13 D03重新分析：直接測量注入的熵——熵確實打在對的點上，GE卻還是變差，是個誠實的開放問題

21天路線圖第2項（跟TVLA平行、不用重跑訓練）：不問「D03的GE有沒有變好」（附錄A.6已經回答過，沒有），改問「D03的三個`noise_std`設定，實際注入了多少熵，而且是不是打在對的地方」。

**方法**：新增`scripts/05_measure_injected_entropy.py`——`AlwaysOnStochasticNoise`每次呼叫都會重新採樣雜訊（不受training flag影響，見附錄A.6/D03設計），所以把「同一批乾淨軌跡」固定住、對同一個已訓練好的`generator.keras`重複呼叫30次，輸出之間唯一的差異來源就是產生器自己注入的隨機性——不需要重新訓練，直接測量已有的checkpoint。在兩個已知洩漏點（附錄A.8的`masked_value` point 517、`mask_value` point 156）算重複呼叫間的逐點變異數，換算成每個時間點的差分熵（bits）。

**結果**：

```
run                          noise_std   masked_value(517)      mask_value(156)
D01（確定性基準）              0.000      std=0.0000 bits=-inf   std=0.0000 bits=-inf   （正對照組，確認方法論本身有效）
D03_noise_low                 0.050      std=0.2989 bits=0.31   std=0.2827 bits=0.22
D03_noise_mid                 0.150      std=0.8846 bits=1.87   std=0.9004 bits=1.90
D03_noise_high                0.300      std=1.7324 bits=2.84   std=1.7524 bits=2.86
```

**理論值（noise_std × epsilon）幾乎完全對上實測值**（0.05×6.0=0.3 vs 實測0.30；0.15×6.0=0.9 vs 實測0.88-0.90；0.30×6.0=1.8 vs 實測1.73-1.75，只在噪訊最大時因為clip邊界有小幅折損）——確認`AlwaysOnStochasticNoise`的行為完全符合設計預期，**熵確實有被注入，而且確實打在這兩個已知的關鍵洩漏點上，不是打在別的地方**。D01的std精確為0（不是接近0，是精確0），驗證了量測方法本身正確（純確定性函數在固定輸入下多次呼叫必須完全一致）。

**但GE並沒有變好**（附錄A.6既有數據）：

```
D01（0 bits）        GE@9000=111.84
D03_noise_low（0.3 bits）  GE@9000=119.54
D03_noise_mid（1.9 bits）  GE@9000=138.30
D03_noise_high（2.85 bits）GE@9000=137.14
```

熵單調上升，GE也單調變差——這跟noisy leakage的直覺（注入更多獨立雜訊應該讓攻擊者需要更多軌跡，也就是GE應該變好）**完全相反**。

**誠實面對這個結果，不強行套一個好聽的解釋**：這次的量測結果否定了原本以為理所當然的解釋——不是「隨機性沒有打在對的點上」，因為現在直接測出來確實有打在對的點上，且量級跟理論值吻合。真正在發生的事情比附錄A.8的「洩漏點覆蓋不完整」故事更複雜：即使在兩個已知的univariate洩漏點上注入了真正、可驗證的獨立熵，這件事本身沒有轉換成攻擊者的GE變差——這代表 E01 這個CNN攻擊者實際依賴的可能不只是這兩個點的簡單Hamming weight相關性（我們的SNR/TVLA診斷用的簡化模型），而是分佈在整條軌跡上更複雜、更難用兩個univariate指標完整刻畫的結構；也可能是產生器整體行為隨`noise_std`改變時，在其他700個點的其餘部分產生了某種副作用，抵銷甚至蓋過了這兩個點的改善。**這裡不下定論，誠實記錄成一個尚未解開的謎，而不是硬套一個沒有驗證過的解釋**——跟D01/D02/D03一路走來的方法論一致：寧可留一個開放問題，也不要用聽起來合理但沒有實測驗證的敘事填空。

**這對報告/論文敘事的意義**：這其實是Route A敘事裡一個很誠實、很有價值的觀察——**局部、univariate的洩漏抑制（不管是靠壓低SNR還是注入熵）不能保證轉換成攻擊者實際需要的軌跡數變多**，這暗示對抗式擾動防禦（不管是GAN還是搭配隨機性）如果只針對簡化的univariate洩漏模型設計/驗證，可能從根本上量錯了攻防的真正互動介面。這比單純的「D03沒用」更深刻，也是方向.md「這不是壞事…你們就從『宣稱安全但說不清楚』變成『精確知道自己防的是什麼、不防的是什麼』」這句話在這裡最直接的體現。

**尚未做**：沒有嘗試推導任何形式化界（noisy leakage的正式定理），這超出本次範圍，方向.md自己也說「大學專題不一定做得完」；也還沒檢查「熵有沒有意外地打在CNN真正依賴、但我們兩個已知洩漏點沒涵蓋到的第三個地方」——這需要更完整的多變量洩漏分析，留待後續決定要不要投入。

### A.14 D05設計：通用擾動模板（universal perturbation）——實作完成，尚未正式跑

21天路線圖第3項（方向.md claim②的唯一前提＋Route A需要的第三種防禦類別對照組）。

**設計**：新增`src/generator/universal_perturber.py::UniversalTemplate`——單一可訓練的`(trace_len, 1)`權重，`tanh`壓到`(-1,1)`後對整個batch原封不動廣播（輸入軌跡的內容完全不影響輸出，只用來讀batch size），再套用D01既有的`BoundedPerturbation`乘上epsilon。跟`conv_perturber.py`的CNN版本共用完全相同的`(trace_len,1)→(trace_len,1)`介面，所以`01_train_defender.py`／`02_generate_defended.py`不需要另外改介面，只需要新增一個`generator.architecture: cnn | universal`設定分派（`configs/base.yaml`預設`cnn`，不影響D01-D04既有行為）。訓練走完全相同的對抗訓練迴圈跟loss（`src/train/adversarial.py`一行都沒改），確保跟CNN版本唯一的差異變因就是「擾動是否隨軌跡內容調整」——PSR成本匹配下的比較才公平。

**為什麼這個架構同時買到兩件事**：
1. **Route A對照組**：per-trace FGSM（Tran式）／universal perturbation（Gu式）／learned generator（我們的CNN版）是方向.md列的三種要比較的防禦類別，這個實作補上中間那一種。
2. **方向.md claim②的唯一前提**：「工程成本比masking低」只有在「不需要每次都跑推論引擎」的前提下才站得住腳——通用模板部署時就是一個固定波形疊加上去，不需要CNN forward pass，跟masked AES gadget或CNN推論引擎的執行期成本完全不同量級，這是唯一能讓這個主張變成純工程算術（而不是空話）的架構選擇。

**已完成**：6個新測試（`tests/test_universal_perturber.py`：輸出形狀、epsilon上界、**跨軌跡輸出完全一致**這個universal的定義性質、zeros初始化下訓練前擾動精確為0、save/load round-trip、梯度確實流到`template`變數），`tests/`現在42個測試全過。小規模smoke test（300條/3epoch，真實E01攻擊者）：訓練confuse loss正常下降（6.49→6.06）、`02_generate_defended.py`正常產出`defended_traces.npy`、餵進攻擊端Stage B介面正常產出`metrics.json`——管線接線正確（此規模下PSR僅0.002，數字本身無意義，純粹驗證接線）。

`configs/exp/D05_universal.yaml`已就緒（沿用D01的loss權重/epsilon/n_train/epochs，只換`generator.architecture: universal`）。**尚未跑正式15000條/40epoch GPU實驗**。

### 下一步

- 正式跑D05（GPU），跟D01（PSR=0.0249, GE@9000=111.84）成本匹配比較——通用模板的容量遠小於CNN（一個700維向量 vs 數萬參數的Conv1D網路），預期同樣的PSR成本下效果可能更差（表達能力不足），但這正是要誠實測出來的東西，不是預設會贏。
- 21天路線圖下一項（D06）是自適應攻擊者（A1），是目前最重的一塊，也是Route A論述的核心。

## 附錄 D：D05 正式跑（GPU，15000條軌跡，通用擾動模板）

### D.1 結果：成本只要D01的39%，GE反而更不收斂——但SNR/TVLA證明這是可以事先用數學證明的假象

`runs/D05_universal_20260817_123955_1140658/`，訓練在epoch 32觸發patience早停。

```
PSR: mean=0.0098（D01是0.0249，只要39%成本）
L2:  mean=7.35
Linf: mean=0.73
```

Stage B正式評估（100-run，max_traces=9000）：

```
GE @ N=9000  = 162.46（比隨機基準127.5還高34.96，比D01的111.84更「偏」）
N_TGE  = None
SR1@9000 = 0.0000
PI = -0.0962
```

**GE曲線隨N遞增，不是打平**：N=1000時141.17，N=9000時162.46，持續往上爬，不像D01的曲線在110-123之間震盪打平。這代表通用模板造成的不是「隨機混淆」，是一種**系統性、會隨軌跡數累加而複合增強的偏誤**——每條軌跡都套用同一個擾動，所以它對決策的影響方向完全一致，多次獨立攻擊的log-likelihood累加後，這個一致的偏誤會被放大，不會像隨機雜訊那樣被平均掉。

### D.2 SNR/TVLA：0.0%改變，不是「幾乎沒改變」，是數學上證明了不可能改變

用附錄A.8/A.11同一套SNR診斷跟TVLA檢查，比較D05 vs 同PSR等級的高斯噪訊（sigma_ratio=0.1，PSR=0.0089，比D05還便宜）：

```
SNR（masked_value point 517）：clean=6.6787  GAN(D05)=6.6787（-0.0%）  Gaussian=6.1770（+7.5%，噪訊本身量太小反而在雜訊層級微幅波動）
SNR（mask_value point 156）  ：clean=1.3316  GAN(D05)=1.3316（+0.0%）  Gaussian=1.3085（+1.7%）

TVLA（masked_value）：clean|t|=71.73  GAN(D05)|t|=71.73  Gaussian|t|=71.27
TVLA（mask_value）  ：clean|t|=52.82  GAN(D05)|t|=52.82  Gaussian|t|=52.32
```

**D05對SNR/TVLA的影響精確是0**（不是接近0，是到小數點後完全相同）。這不是巧合，是**可以直接證明的數學必然**：通用擾動對每一條軌跡加上完全相同的常數波形，而SNR＝Var(各類別平均)/mean(類內變異數)——對每個樣本加同一個常數c，類內變異數`Var(X+c)=Var(X)`完全不變，各類別平均的變異數`Var(mean_1+c, mean_2+c,...)=Var(mean_1, mean_2,...)`也完全不變，SNR因此**精確不變**（TVLA用的Welch t-test同理，只依賴組內變異數跟組間均值差，對常數平移一樣不變）。這是附錄A.6/D03測試設計時發現的「平方相關係數對仿射變換不變」那個數學性質的推廣版——**任何不依賴軌跡內容的固定擾動，天生就不可能改變SNR或TVLA的偵測結果，這件事不需要做實驗才知道，實驗只是驗證了理論预测是對的**。

### D.3 綜合解讀：這是目前為止對Route A論述最乾淨的一組證據

D05同時做到兩件事，而且是有數學保證的：**在SNR/TVLA意義下精確地什麼都沒做（可證明），卻讓GE比D01更不收斂、比隨機基準還糟34.96、且隨軌跡數持續惡化**——這比D01跟高斯噪訊的比較更乾淨地展示了附錄A.7的核心論點：GE對單一凍結模型的表現，可以跟「有沒有真的破壞資訊」完全脫鉤，這裡甚至不需要爭論「有沒有充分壓低SNR」，通用擾動連定義上都不可能壓低SNR，卻仍然能在GE指標上「看起來」比D01的CNN還有效。

**這對報告/論文敘事的意義**：如果只看GE，會得出「D05比D01更好、更省成本」這個危險的錯誤結論；但SNR/TVLA診斷直接、可證明地戳破這個錯覺——這正是方向.md一開頭強調的那個不對稱（「安全性主張只能被證偽」）最直接的示範案例，而且是本專案目前唯一一個**不需要靠實驗歸納、靠純數學論證就能斷言「這個防禦對安全性毫無貢獻」**的案例，比D01/高斯的比較更適合放進報告當作方法論教學範例：GE看起來的「贏」跟真正的安全性可以完全對不上號，而且原因在這裡是可以嚴格證明的，不是靠一堆實驗歸納出的經驗猜測。

### 下一步

- 21天路線圖下一項：D06自適應攻擊者（A1）——用防禦後的A集重訓一個攻擊者，這是目前唯一還沒被SNR/TVLA診斷「戳破」過的評估角度，值得投入。
- D05這個結果也連帶說明：**任何只看GE、不搭配SNR/TVLA的防禦效果聲稱都不可信**——報告寫作時這點需要明確強調，之後如果要做D07（三方比較圖），必須把SNR/TVLA一起放進圖表，不能只畫PSR-vs-GE。

## 附錄 E：D06 自適應攻擊者（A1）——D01防禦在重訓練下幾乎完全失效

21天路線圖第4項，Route A論述的核心（統一適應性攻擊協定）。跟攻擊端的介面：`dlsca-attack-v2` 新增 `scripts/01_train_attacker.py --profiling-traces PATH`（見攻擊端CLAUDE.md B.55），用 `06_generate_defended_trainset.py` 對全部50000條Profiling_traces套用D01的產生器，輸出`defended_profiling_traces.npy`，餵給攻擊端重新訓練一個全新的攻擊者（架構/超參數/seed跟`E01_baseline_clean`完全相同，唯一差異是訓練資料是否被防禦過）。

### E.1 結果：不只是「有改善」，是幾乎完全復原到未防禦水準

用同一批D01防禦後的E集軌跡（frozen E01評估時用的那一批），比較兩種攻擊者：

| 情境 | 攻擊者 | GE@9000 | N_TGE | SR1@9000 |
|---|---|---|---|---|
| **A0（靜態，凍結E01）** | 沒看過防禦後的資料 | **111.84** | None（不收斂） | 0.00 |
| **A1（自適應，用D01防禦後的A集重訓）** | 訓練時就看過防禦後的資料 | **0.00** | **596** | **1.00** |
| 對照：E01在乾淨（未防禦）E集上的原始表現 | 凍結E01 | 0.00 | 475 | 1.00 |

**自適應攻擊者完全破解了D01的防禦**——GE曲線平滑收斂（N=500時2.93，N=596時0.99，N=1000後精確等於0，之後全程維持0），100次獨立攻擊全部成功。而且N_TGE=596只比**完全沒有防禦時的475多25%**——意思是D01這個防禦，一旦攻擊者知道防禦存在並重新訓練，幾乎不構成任何實質障礙，只是讓攻擊者多花約120條軌跡。

### E.2 這對整個防禦敘事的意義

跟附錄D（D05的SNR/TVLA診斷）合起來看，現在有兩條完全獨立、方法論不同的證據指向同一個結論：

1. **D05（SNR/TVLA診斷）**：靜態情境下「看起來贏」的GE數字，可以跟真正的資訊破壞完全脫鉤（可證明）。
2. **D06（自適應攻擊者）**：即使是D01這個「有一點真實SNR壓制」的配方（附錄A.8），只要攻擊者知道防禦存在並重新訓練，防禦幾乎立刻瓦解，回到接近未防禦的破解成本。

**這兩條證據分別戳破了「靜態評估」的兩種盲點**：一種是評估方法本身不夠嚴謹（GE可以無視底層資訊仍然「看起來」有效，D05示範），另一種是威脅模型本身不夠嚴謹（凍結攻擊者假設過於樂觀，D06示範）。合起來完整支撐Route A的核心論點：「對抗式擾動防禦在統一的適應性攻擊協定下普遍失效，而且失效是資訊理論/威脅模型層級的，不是工程細節沒調好」。

**跟方向.md的對照**：方向.md原文警告「更有說服力但工程量大」，實測下來工程量其實不大（複用既有的攻擊端訓練pipeline，只加一個CLI參數），但說服力確實如預期地強——這是本專案目前為止對核心論點支撐力道最強的一組結果，比D01-D05的PSR-vs-GE比較都更直接、更難反駁。

### 下一步

- 21天路線圖：TVLA✓、D03重分析✓、D05通用模板（設計+跑完+診斷）✓、D06自適應攻擊者✓——**四個核心項目全部完成**，剩下的時間應該轉向整理報告草稿（roadmap第5項）。
- 如果還有餘裕：可以對D05（通用模板）也做一次同樣的A1測試，預期會更快被攻破（因為連SNR都沒壓低，順理成章預期比D01更容易被適應——但這個預測本身也值得驗證，不要假設）。
- 也可以測「用D01訓練出來的防禦，重訓練的攻擊者是否連對『不同PSR成本』的其他防禦（例如高斯σ=0.25）也一併免疫」——如果是，代表適應性不僅破解特定防禦，還可能有跨防禦的泛化能力，這對報告會是更強的論點，但目前還沒測。

## 附錄 F：重大修正——D01 vs 高斯的GE比較方向從一開始就錯了

**這節必須放在最前面看，不能跳過**：附錄A.3到E的敘事（「GAN輸給高斯噪訊」「防禦效率不如預期」）全部建立在一個方向錯誤的比較上。以下是修正過程跟修正後的正確結論。

### F.1 錯誤是什麼

`dlsca-attack-v2/src/attack/keyrank.py::evaluate`：`rank = (cum_score > cum_score_correct_key).sum()`——正確金鑰排名0＝攻擊者第一名猜中＝**攻擊成功**。攻擊端CLAUDE.md自己的定義也講得很清楚：「GE(N)｜100次rank的平均｜主指標，**越低越接近破解**」。也就是說：**GE越低＝金鑰越接近被破解＝對防禦端而言越糟；GE越高（或N_TGE=None，代表GE從未在測試視窗內持續壓到1以下）＝對防禦端而言越好**。

附錄A.3把「高斯噪訊GE@9000=0.00（100次攻擊全部收斂）」講成「高斯讓攻擊者完全瓦解」，把這個當成高斯「贏」的證據；但GE=0.00根本就是**攻擊完全成功、金鑰被破解**，不是攻擊者被瓦解。反過來，D01的GE@9000=111.84、N_TGE=None，才是**攻擊在測試視窗內從未成功過**——這才是防禦端想要的結果。整個方向從A.3就反了，並且一路帶到A.4（D02）、A.6-A.7（D03）、A.13（熵重分析）的敘事裡。

### F.2 交叉驗證，確認不是我看錯

- **攻擊端自己的C.3小節原話**：「sigma_ratio≈1.5（PSR≈0.134）是這條曲線上第一個讓攻擊者連9000條軌跡都攻不破的門檻」——白紙黑字承認sigma<1.5時高斯噪訊**沒有**擋住攻擊者（只是拖慢）。
- **攻擊端整個B系列附錄**：E01的N_TGE=475從頭到尾都被當成攻擊方的**成功**指標（「本專案目前已知最好、且唯一被證實真的能完全破解金鑰的配方」），後續所有調參（B.7-B.16）都是想辦法把N_TGE往下壓（讓攻擊更快成功）。這代表「N_TGE小＝攻擊強」「N_TGE大或None＝攻擊弱／防禦強」的方向，在攻擊端自己的敘事裡從未反過。

### F.3 修正後的正確結論

用「N_TGE=None（在9000條軌跡內從未被持續攻破）」當防禦成功的主要判準，重新看所有已跑過的配方：

| 防禦 | PSR | N_TGE | 是否在9000條內守住 |
|---|---|---|---|
| 高斯 σ=0.1 | 0.0089 | 514 | 否（被攻破） |
| 高斯 σ=0.25 | 0.0223 | 775 | 否（被攻破） |
| 高斯 σ=0.5 | 0.0446 | 2276 | 否（被攻破） |
| 高斯 σ=0.75 | 0.0668 | 5080 | 否（被攻破） |
| 高斯 σ=1.0 | 0.0891 | 7019 | 否（被攻破） |
| **高斯 σ=1.5（攻擊端定義的成功門檻）** | **0.1337** | **None** | **是** |
| 高斯 σ=2.0 / 3.0 | 0.1783 / 0.2674 | None | 是 |
| **D01** | **0.0249** | **None** | **是（比高斯門檻便宜5.4倍）** |
| D02全部4點、D03全部3點 | 0.0341–0.1493 | 全部None | 全部守住 |
| D05 | 0.0098 | None | 是（比高斯門檻便宜13.6倍） |

**D01跟D02/D03/D05的每一個變體，全部都在9000條軌跡內守住了防禦，而且大多數成本都遠低於高斯自己需要的門檻（PSR=0.1337）**。這不是「GAN輸給高斯」，是**GAN類防禦用遠低於高斯所需的成本，達到了跟高斯同等（甚至更早）的『在測試視窗內無法被攻破』這個結果**——這正是本專案最初想證明的假設（攻擊端CLAUDE.md附錄C.3原文：「GAN 防禦如果能用遠低於 PSR=0.134 的成本達到同等（甚至更好）的防禦效果，就是『效率更好、成本更低』這個論點的直接量化證據」）。

### F.4 什麼東西沒有變、甚至變得更有說服力

TVLA跟D06完全不依賴GE的方向，這兩個發現**原封不動成立**，而且重新連起來後故事更扎實：

- **TVLA（附錄A.11/A.12）**：D01「守住」了對E01這一個特定frozen模型的攻擊（N_TGE=None，比高斯門檻便宜5.4倍），但TVLA顯示底層model-agnostic的洩漏幾乎沒動（|t|=67.61 vs clean的71.73，只降5%，還是門檻15倍）。**這代表D01的『守住』完全是針對E01這一個模型的決策邊界討便宜，不是真的破壞了資訊**——GE看起來的巨大勝利（5.4倍成本效率）跟真正的安全性完全脫鉤，TVLA把這件事戳破了。
- **D06（附錄E）**：自適應攻擊者重訓練後，N_TGE從None掉回596（幾乎等於完全無防禦的475）。**如果D01是真的靠資訊破壞守住，攻擊者重訓練不應該這麼容易就打回原形**——D06直接證實了TVLA的懷疑：D01的『成功』是針對特定模型的假象，換一個知情的攻擊者就破功。

**修正後的核心論點反而更犀利**：不是「GAN防禦效率不如高斯」，是「**GAN防禦類方法，用GE這個指標來看甚至可以『贏』過高斯噪訊5倍以上的成本效率——但這個勝利本身就是危險的假象，TVLA跟自適應攻擊者從兩個獨立方向戳破了它**」。這比原本「GAN單純輸掉」的敘事更貼近方向.md一開始強調的重點（「安全性主張只能被證偽」），也是更強的方法論教訓：**一個防禦不只可能在GE上小贏卻是假象，還可能贏得非常誇張（5倍效率）卻更是假象**。

### F.5 D02/D03的解讀需要重新開放，不能沿用舊結論

附錄A.4/A.6原本說「拿掉正則化/加隨機性讓GE變差，代表正則化是必要條件」——這個結論建立在「GE變差＝防禦變弱」的錯誤方向上。**修正後：D02/D03全部7個變體都有N_TGE=None，全部都「守住」了**，GE數值從111.84到197.76的差異，究竟代表：

(a) 数值越高＝真的壓制更多資訊（更強的防禦），還是
(b) 数值越高＝越像D05那種「confidently wrong」的模型特定假象（附錄D已證明D05的GE=162.46在SNR/TVLA上是精確0%效果）——

**這個問題目前沒有答案，因為D02/D03的其他變體都沒有做過SNR/TVLA檢查**（只有D01跟D05做過）。在補做這些檢查之前，不能再宣稱「正則化更好」或「正則化更差」，只能誠實說：全部7個變體都是有效的靜態防禦，但相對強弱、以及是否為假象，需要TVLA才能回答，這是新的待辦事項（見附錄F.7）。

### F.6 附錄A.13（D03熵重分析）的「謎題」其實不是謎題

原文把「熵上升、GE也上升（119.54→138.30→137.14，原文稱為『變差』）」當成違反noisy leakage直覺的開放問題。**修正後：GE上升＝防禦變強，這其實完全符合noisy leakage的直覺**（注入更多獨立熵，攻擊者應該更難）——原本的「謎題」是建立在把GE上升誤讀成「變差」上，根本不存在矛盾。真正還沒回答的問題變成：這個隨熵上升的GE強化，是否經得起TVLA檢驗（會不會也是D05那種假象），這點還沒測過。

### F.7 下一步（取代原本A.10/D.4/E.3的下一步規劃）

1. **對D02/D03的7個變體、以及D04（見附錄G）補做TVLA檢查**——確認GE的相對高低，究竟哪些是真實資訊破壞、哪些是D05式的模型特定假象。這是最優先的待辦，直接影響「哪個配方最好」這個問題能不能被誠實回答。
2. **修正README、docs/runs.md、已發布的報告artifact**——所有「D01效率不如高斯」「防禦效率明顯更差」的表述都需要更新為修正後的正確方向。
3. 重新評估D07（三方比較圖）的必要性——原本的動機（找到贏過高斯的配方）現在看來D01/D05在GE意義下早就「贏」了，圖表的真正價值應該是同時疊上TVLA/SNR，展示「GE贏≠真安全」這個核心教訓，而不是單純比較PSR-vs-GE。

## 附錄 G：D04 正式跑——修正方向後，是目前TVLA表現最好的配方

`runs/D04_snr_aware_20260817_134828_1174647/`，訓練跑滿40 epoch沒有觸發早停（`leak_masked_value`/`leak_mask_value`兩項loss在後段穩定在0.21/0.30附近，沒有繼續下降但也沒有反彈）。

### G.1 成本與GE：守住防禦，成本比D01高

```
PSR: mean=0.0459（D01的1.8倍）
GE @ N=9000 = 130.37（N_TGE=None，守住）
```

### G.2 SNR/TVLA：目前所有配方裡壓制效果最好的一組

跟同PSR等級的高斯（sigma_ratio=0.5，PSR=0.0446）比較：

```
SNR（masked_value point 517）：clean=6.68  D04=0.12（-98.1%！）  Gaussian=2.36（-64.7%）
SNR（mask_value point 156）  ：clean=1.33  D04=0.63（-52.8%）    Gaussian=0.87（-34.9%）

TVLA（masked_value）：clean|t|=71.73  D04|t|=20.56（門檻4.5的4.6倍）  Gaussian|t|=61.66（門檻的13.7倍）
TVLA（mask_value）  ：clean|t|=52.82  D04|t|=44.43（門檻的9.9倍）    Gaussian|t|=46.35（門檻的10.3倍）
```

**D04在masked_value這個主要洩漏點上，SNR壓制幅度是Gaussian的1.5倍（98% vs 65%），TVLA的|t|從clean的71.73壓到20.56——是目前為止測過的所有配方（D01/D02/D03/D05/高斯全系列）裡，唯一一個把TVLA數字壓到門檻10倍以內的**（D01是15倍，D05是完全不變，高斯σ=0.5是13.7倍）。仍然沒有真正過門檻（20.56 vs 4.5），但是朝正確方向邁出目前最大的一步。

### G.3 這證實了附錄A.9的loss設計是對的方向

附錄A.9把loss從「只讓E01單次query出錯」換成「直接壓低已知洩漏點的可分性」，當時只做了smoke test。**正式跑的結果證實這個設計改動確實有效**——跟D01（confuse-only）比，D04在真正的model-agnostic指標（SNR/TVLA）上都大幅超前，不是像D02/D03那樣「換個旋鈕、GE變好看但沒有真正的機制支撐」。

**但也要誠實面對代價**：D04的PSR成本是D01的1.8倍，而且即使TVLA數字明顯改善，仍然沒有真正低於門檻——加上還沒有把D04拿去做D06那種自適應攻擊者測試，不知道這個「更真實的壓制」能不能撐過重新訓練。

### G.4 下一步

- **對D04做自適應攻擊者測試（D06流程重複一次）**：D01在GE意義下「贏」高斯5.4倍，卻在自適應攻擊下幾乎完全瓦解；D04在TVLA意義下明顯比D01更接近真安全，理論上應該更能撐過自適應攻擊——這是目前最值得驗證的假設，直接關係到「SNR-aware loss這條路是否值得繼續投入」這個問題。
- 對D02/D03的7個變體補做SNR/TVLA（附錄F.7第1項），跟D04放在同一張表上比較，才能知道D04的改善幅度在所有已測配方裡排第幾。

## 附錄 H：D04的自適應攻擊者測試——第一個撐過重訓練的配方

附錄F.7第1項：D01在D06裡幾乎完全被自適應攻擊者攻破（N_TGE從None掉回596）。D04在TVLA上明顯優於D01（附錄G），理論上應該更能撐過重訓練——用完全相同的流程測試（`06_generate_defended_trainset.py`對D04產生器套用全部50000條profiling traces，重訓一個攻擊者，`runs/E01_adaptive_A1_retrain_20260817_141416_1187724/`）。

### H.1 結果：D04撐住了，D01沒有

| 情境 | 攻擊者 | GE@9000 | N_TGE |
|---|---|---|---|
| D01（凍結E01） | 沒看過防禦後資料 | 130.37* | None |
| D01（自適應，附錄E） | 用D01防禦後資料重訓 | 0.00 | **596（幾乎完全打回原形）** |
| D04（凍結E01） | 沒看過防禦後資料 | 130.37 | None |
| **D04（自適應，本節）** | **用D04防禦後資料重訓** | **124.17** | **None（沒有打回原形）** |

*（D04對凍結E01的GE剛好也是130.37，跟D01的自適應攻擊者對比列放在一起純屬巧合，數值來自附錄G）

**D04的GE曲線在整個評估窗口（N=1000到9000）穩定維持在124-138之間，沒有任何收斂跡象**（N=1000時132.20，N=9000時124.17，中間微幅震盪但不是趨勢）——訓練期間20-run快速預覽也顯示同樣的模式：50個epoch裡GE在97.90到185.30之間反覆震盪，**從未達到任何一次持續的N_TGE**（相較之下D01的自適應攻擊者在epoch15就已經拿到N_TGE=674，並持續進步到epoch40的245）。

### H.2 這是本專案第一個通過雙重驗證的配方

D01拿到過「靜態守住＋GE贏高斯5.4倍」的漂亮數字，但TVLA跟自適應攻擊者兩條獨立證據都戳破了它。**D04是目前唯一同時通過這兩關的配方**：TVLA顯示它做出了目前最好的真實洩漏抑制（附錄G，masked_value頻道|t|降到20.56），現在自適應攻擊者測試證實這個抑制強到讓一個拿到全部50000條防禦後訓練資料、訓練足足50個epoch的攻擊者都無法可靠地重新學會攻擊。

**沒有過度誇大的空間，誠實列出還沒驗證的部分**：
- D04仍然沒有真正通過TVLA的|t|<4.5門檻（20.56/44.43都還在門檻之上），只是比其他配方更接近；「撐過自適應攻擊」不等於「達到形式化安全」。
- 只跑過一次自適應重訓練（50 epoch，跟D01附錄E同款設定），沒有掃描過給攻擊者更長訓練時間、或換架構（cnn_best/resnet）重訓是否仍然守得住。
- PSR成本是D01的1.8倍，仍需要在最終報告裡誠實地把「效果」跟「成本」放在一起討論，不能只講效果面。

### H.3 對整份報告的意義

這是Route A論述裡第一個「正面」的結果——不是又一個被戳破的假象，是一個目前看來真的有實質防護力的配方，而且是從系統性除錯（附錄A.8的機制診斷 → 附錄A.9的loss重新設計 → 附錄G的TVLA驗證 → 本節的自適應驗證）一路推導出來的，不是碰運氣。**建議把D04的完整推導鏈（A.8→A.9→G→H）當作報告/論文最核心的正面敘事主線**，D01/D02/D03/D05則是這條主線之前的必要鋪墊（誠實記錄每一步為什麼失敗、學到了什麼）。

### 下一步

- 對D02/D03其餘6個變體、以及D05補做自適應攻擊者測試，看D04的「撐住」是不是特例還是這類loss設計都能達到（如果D02_noreg之類的高GE配方也撐得住，代表可能不需要特別做D04那種洩漏感知loss，只是需要正則化夠弱；如果撐不住，代表D04的loss重新設計才是關鍵，不是巧合）。
- 換一個攻擊架構（cnn_best/resnet）重複自適應測試，確認D04的防護力不是只對cnn_light這個特定架構有效。

## 附錄 I：D02/D03其餘6個變體補做TVLA——D02全部是假象，D03隨熵量呈現真實的劑量反應

附錄F.7第2項：對D02四個變體、D03三個變體，用附錄A.11/A.12同一套bit-split TVLA（跟matched-cost高斯噪訊對照），補齊只有D01/D04/D05做過的檢查缺口。

### I.1 結果

| 變體 | PSR | masked_value \|t\| | mask_value \|t\| | GE@9000（已有數據） |
|---|---|---|---|---|
| 乾淨對照組 | — | 71.73 | 52.82 | — |
| D01 | 0.0249 | 67.61 | 52.98 | 111.84 |
| D02_nosmooth | 0.0393 | 68.33 | 51.00 | 137.32 |
| D02_lowreg | 0.0636 | 71.10 | 52.92 | 194.83 |
| D02_nol2 | 0.0974 | 67.53 | 52.84 | 187.70 |
| D02_noreg | 0.1493 | 70.53 | 53.21 | 197.76 |
| D03_low（noise_std=0.05） | 0.0341 | 67.74 | 51.98 | 119.54 |
| D03_mid（noise_std=0.15） | 0.0610 | 61.76 | 45.94 | 138.30 |
| **D03_high（noise_std=0.30）** | **0.1072** | **47.43** | **43.42** | 137.14 |
| D04 | 0.0459 | 20.56 | 44.43 | 130.37 |

### I.2 D02全部4個變體：GE高但TVLA幾乎不變，證實是假象

**D02的四個變體（拿掉/降低正則化）在TVLA上幾乎跟clean沒有差別**（masked_value全部落在67.5-71.1，只比clean少0.6-4.2；mask_value甚至有兩個變體比clean還高）——**儘管D02_noreg花了D01六倍的PSR成本、GE甚至衝到197.76（附錄A.4曾經誤讀成「防禦更強」），TVLA顯示它幾乎沒有壓制任何真實洩漏**。這證實了附錄F.5提出的疑問：D02系列高GE值全部是D05式的model-specific假象，不是真實安全性——花更多成本、GE衝更高，換到的其實是更誇張地鑽E01這一個模型的決策邊界漏洞，不是更多的資訊破壞。

### I.3 D03三個變體：呈現清楚的劑量反應，隨機性確實有真實效果——只是沒反映在GE上

**D03三個變體隨noise_std呈現單調、真實的TVLA改善**：0.05時幾乎沒變化（67.74，等於D01水準），0.15時開始有感（61.76），0.30時masked_value降到47.43——**是除了D04以外壓制最多的配方，比D01整整少了20.18**。

**這跟附錄A.6/A.13觀察到的「GE隨noise_std上升反而變差」形成鮮明對比**：D03_high的GE（137.14）比D01（111.84）差，但TVLA的真實壓制效果卻明顯比D01好。這代表**附錄A.7原本猜測的noisy leakage直覺（注入熵應該讓攻擊更難）其實部分是對的**——熵確實在破壞真實資訊（TVLA證實），只是這個真實的資訊破壞沒有讓GE對E01這一個特定凍結模型變好看。GE會被model-specific的假象污染，TVLA不會，這是兩者第一次在同一組數據上出現方向不一致的直接證據。

### I.4 這對「D04的成功是不是特例」這個問題的初步回答

D03_high證明：**單純加大隨機性（不用D04那種洩漏感知loss），也能買到真實、可觀的TVLA改善**，只是幅度遠不如D04（47.43 vs 20.56）。這代表D04的loss重新設計不是唯一有效的路線，但目前看來是效率最高的一條——同樣或更低的PSR成本，換到遠更多的真實壓制。

**下一步**：D03_high雖然TVLA比D01好，GE卻更差（137.14 vs 111.84）——它撐得住自適應攻擊者嗎？如果撐得住，代表「單純隨機性」也是一條有效路線，不需要loss重新設計這麼複雜；如果撐不住，代表D04的loss設計仍然是關鍵，不是隨機性本身。這是接下來要驗證的問題（見附錄J，如果已經跑完）。

## 附錄 J：D03_high的自適應攻擊者測試——真的有效果，但撐得比D04弱得多

附錄I結尾提出的問題：D03_high（純隨機性，noise_std=0.30，沒有D04那種洩漏感知loss）雖然TVLA有真實改善（masked_value降到47.43），但能不能像D04一樣撐過自適應攻擊者？用完全相同的流程測試（`runs/E01_adaptive_A1_retrain_20260817_142852_1207814/`）。

### J.1 結果：N_TGE仍是None，但GE曲線正在明顯下滑

```
N_TGE = None，GE@9000 = 17.99（比D04的124.17低得多）
```

GE曲線隨N持續下降，不是打平：

```
N=1000: 83.63
N=3000: 55.99
N=5000: 39.62
N=7000: 26.56
N=9000: 17.99
```

技術上N_TGE仍然是None（GE還沒有在9000條軌跡內持續壓到1以下），跟D04一樣「守住」了——**但曲線形狀完全不同**：D04在整個評估窗口內穩定維持在124-138（沒有下滑趨勢），D03_high則是從83.63一路穩定下滑到17.99，明顯正在被攻擊者一步步攻破，只是還沒攻完。合理推測如果評估窗口拉寬到15000-20000條軌跡，D03_high很可能會真的收斂（被完全攻破），而D04不會。

### J.2 對「D04的成功是不是特例」的明確回答：不是特例，但也不是隨便加隨機性就能達到

D03_high證實：**單純加大隨機性確實買到了真實的TVLA改善（附錄I），這個改善也確實在自適應攻擊下提供了比D01更多的抵抗力**（D01在epoch15就被攻破到N_TGE=674，D03_high到epoch50都還沒被攻破）——**但這個抵抗力遠不如D04**（D04的GE曲線完全平坦，D03_high則是明顯正在瓦解中）。

**這給出一個清楚、有層次的排名**：D01（幾乎立刻被攻破）< D03_high（撐比較久，但正在瓦解）< D04（目前為止看不出瓦解跡象）。這代表**D04的loss重新設計確實提供了比單純隨機性更多的東西**，不只是「有沒有隨機性」這麼簡單——洩漏感知的loss設計（附錄A.9）本身是有實質貢獻的，不能用「反正加隨機性就好」取代。

### J.3 對報告敘事的意義

三層對照現在完整了：D01（無真實效果，GE假象，自適應下秒破）→ D03_high（部分真實效果，自適應下延後但仍在瓦解）→ D04（最多真實效果，自適應下目前未見瓦解跡象）。**這是一條乾淨的劑量-效果關係，從「完全是假象」到「部分真實」到「目前最接近真安全」**，比單一個D04的正面結果更有說服力，因為它排除了「D04只是運氣好」的可能性——效果強度（TVLA壓制幅度）跟抵抗力（自適應攻擊者能不能攻破）呈現一致的順序。

### 下一步

- 這條「TVLA壓制幅度 vs 自適應抵抗力」的單調關係值得再多一兩個資料點驗證（例如D03_mid），但目前三點已經足夠支撐「效果強度決定抵抗力，不是運氣」這個結論，可以視時間決定要不要再補。
- 拉寬D03_high的評估窗口（用`--override attack.max_traces=...`拉到15000+條軌跡，攻擊端E集只有10000條，需要先確認能否支援更大窗口）確認它是否真的會收斂，量化「D03_high比D01晚多久被攻破」這個具體數字。

## 附錄 K：D07——GE-vs-TVLA三方比較圖，不需要新實驗，純粹是把附錄A/D/F/G/I/J的數字疊到同一張成本軸上

附錄F.7第3項規劃的D07，門檻（「D01-D06找到至少一個能打平/贏過高斯基準線的配方」）在附錄F.3就已經滿足（D01/D02/D03/D05全部N_TGE=None，成本大多遠低於高斯自己的門檻PSR=0.1337），且附錄F.7第3項已經把D07的真正價值重新定義為「疊上TVLA/SNR展示GE贏≠真安全」，而不是單純比PSR-vs-GE——這件事不需要新的GPU訓練，全部數字都已經在附錄A.4/A.6/A.12、附錄D、附錄G.2、附錄I.1、附錄E/H/J裡，純粹是彙整跟視覺化。

### K.1 圖表內容

雙排圖表，共用同一條PSR（log刻度）X軸：
- **上排**：GE@9000 vs PSR，高斯8點連線 + GAN家族10個變體（D01/D02×4/D03×3/D04/D05）散點，虛線標示隨機基準127.5。
- **下排**：TVLA masked_value頻道worst\|t\| vs PSR（同一條X軸，方便垂直對齊同一個防禦在兩種評估法下的落點），虛線標示無防禦基準71.73跟安全門檻4.5。
- 下方另附一張三配方（D01/D03_high/D04）的自適應攻擊者對照小表，把附錄E/H/J的結果跟TVLA判定放在同一張表。

### K.2 視覺化後最直接的觀察

把兩排圖疊在一起看，D01在上排的位置（GE=111.84，明顯高於高斯同PSR區間的點）跟下排的位置（\|t\|=67.61，幾乎緊貼著高斯自己的曲線、離門檻還差15倍）形成強烈對比——**同一個X座標，上排說「這裡贏了」，下排說「這裡幾乎什麼都沒發生」**。D05在下排精確疊在「無防禦」那條虛線上（附錄D.2證明的0.0%效果），但上排卻是GE最高（最像「贏」）的點之一——兩排之間的落差在D05身上最誇張，因為這是唯一一個有數學證明保證TVLA不可能改變、GE卻能被training出來明顯偏離基準的案例。全部配方裡只有D04在下排真正往下移動了一段可見的距離（\|t\|=20.56，門檻的4.6倍），儘管它在上排的GE（130.37）遠不是最戲劇性的數字（輸給D02_noreg的197.76）。

### K.3 產出

發布為獨立artifact（不合併進主報告，因為主報告已經很長，這張圖是一個獨立可以單獨分享的「一圖勝過十個附錄」摘要），連結見對話紀錄；本節記錄的是內容跟結論，不是操作紀錄。

### 下一步

- D07完成後，21天路線圖 + 追加的完整驗證鏈（TVLA全掃描、D04雙重驗證、三層對照）已經全部做完，比原計畫超前很多。剩下的候選項目（依附錄F.7/README現況）：D08時域干擾基準線、補齊D02/D05的自適應攻擊者測試、攻擊架構廣度（cnn_best/resnet下的D04重複驗證）、硬體Route C demo——這些互相獨立，可以視剩餘時間排序投入。

## 附錄 L：補齊D02/D05的自適應攻擊者測試——D05符合預期，D02_noreg是一個意外、打破了TVLA-vs-抵抗力的單調關係

附錄K的「下一步」跟D07表格裡標記「未測」的兩格：D02_noreg（GE系列裡PSR最高、TVLA判定「幾乎假象」的配方）跟D05（通用擾動模板，SNR/TVLA數學證明0%效果）。用附錄E/H/J同一套流程（`06_generate_defended_trainset.py`對全部50000條Profiling_traces套用各自的產生器→攻擊端`01_train_attacker.py --profiling-traces`重訓→`02_run_attack.py --traces`對同一批各自的`defended_traces.npy`（E集）跑推論→`03_evaluate.py`正式100-run評估）。

### L.1 結果

| 配方 | PSR | 靜態GE@9000（凍結E01） | TVLA masked\|t\|判定 | **自適應 N_TGE** | 自適應 GE@9000 | 自適應 SR1@9000 |
|---|---|---|---|---|---|---|
| 無防禦（對照） | 0 | — | 71.73 | 475 | 0.00 | 1.00 |
| D01 | 0.0249 | 111.84 | 67.61（幾乎假象） | 596 | 0.00 | 1.00 |
| **D02_noreg** | **0.1493** | 197.76 | 70.53（幾乎假象） | **6268** | 0.04 | 0.96 |
| D03_high（附錄J，已有） | 0.1072 | 137.14 | 47.43（部分真實） | None（未在9000內收斂，但曲線83.63→17.99持續下滑） | 17.99 | — |
| D04（附錄H，已有） | 0.0459 | 130.37 | 20.56（目前最佳） | None（124–138全程持平，無下滑趨勢） | 124.17 | — |
| **D05** | **0.0098** | 162.46 | 71.73（數學證明0%效果） | **597** | 0.00 | 1.00 |

### L.2 D05：完全符合預測

D05的自適應N_TGE=597，幾乎精確等於D01的596、也幾乎等於無防禦基準的475（只多約26%）——**這正是附錄D.2「數學證明0%效果」該有的行為**：既然對SNR/TVLA的影響精確是0，用防禦後的資料重訓攻擊者，效果應該跟直接用接近乾淨的資料訓練一樣，幾乎不比D01（同樣秒破）更難。這個結果沒有意外，是一次乾淨的預測驗證。

### L.3 D02_noreg：意外——TVLA判定「幾乎假象」，但自適應抵抗力遠超D01，甚至逼近D03_high

**這是本次調查裡第一個TVLA判定跟自適應攻擊者結果明顯不一致的案例**。D02_noreg跟D01同樣被TVLA判定為「幾乎假象」（\|t\|都在67-71之間，遠高於門檻），如果附錄J建立的「TVLA壓制幅度決定自適應抵抗力」單調關係成立，D02_noreg應該跟D01一樣幾乎秒破——**但實測N_TGE=6268，是D01的10.5倍，比D03_high還更晚收斂**（D03_high在9000條窗口內都還沒完全收斂，D02_noreg卻在6268條就收斂到底）。

**這打破了附錄J原本建立的簡單排序**，需要誠實面對兩種可能的解釋，目前都還沒有進一步的實驗可以區分：

1. **PSR量級本身可能提供了TVLA量不到的保護**：D02_noreg的PSR（0.1493）是D01的6倍、是全部GAN變體裡最高的，接近D03_high（0.1072）。bit-split TVLA是單點、單bit的統計檢定，只捕捉「這個時間點、這個bit」層級的線性可分性；一個振幅夠大的擾動，即使沒有改變任一單點的類間/類內變異比（因此TVLA數字不變），仍然可能透過**多點/非線性的方式**讓攻擊者的優化問題變得更難解——這跟附錄A.7提出的「這可能是對抗樣本問題」假說沒有被完全推翻有關：對抗擾動可能同時具備「讓凍結模型出錯的脆弱性利用」（附錄F證明是假象）**和**「單純因為振幅夠大而增加訓練難度」（這次D02_noreg可能展示的）兩種機制，只是後者在D01/D02其他變體上PSR不夠高，沒有明顯展現。
2. **也可能只是訓練不穩定/優化困難的副作用**：train_history顯示D02_noreg訓練出的攻擊者train accuracy長期偏低（0.0139，明顯低於D01同等級模型的表現），可能不是「資訊真的變難萃取」，而是「這個振幅的擾動讓CNN訓練本身變得不穩定」，用不同的訓練超參數（例如更長的epoch數、不同的初始化）重跑攻擊者，也許能把這個N_TGE壓回接近D01的水準——這種情況下，6268是攻擊端訓練不夠好的產物，不是防禦真的更強。

**這個問題目前沒有答案，需要更多實驗才能區分**（例如：用不同種子/超參數重跑D02_noreg的adaptive攻擊者，確認6268是否穩定；或對PSR相近但TVLA判定不同的D03_high/D02_noreg做更細緻的比較）。**在此之前，不能宣稱「D02_noreg比D04更好」**——D02_noreg最終還是在9000條窗口內被完全攻破（GE@9000=0.04，SR1=0.96），而D04從未顯示任何收斂跡象，這仍然是質性上的差異，D04依然是目前唯一「看不出被攻破跡象」的配方。但D02_noreg的結果確實說明：**TVLA判定「幾乎假象」不等於「對自適應攻擊者完全沒有額外抵抗力」**，二者是相關但不完全對應的兩件事——附錄J建立的單調排序需要加註：這個排序目前只在D01/D03_high/D04三點上驗證過，加入D02_noreg後不再是簡單的單調關係。

### L.4 修正後的完整排序（按「多晚被攻破」，不是按TVLA數字）

```
D01（596）≈ D05（597，數學上零效果的預測驗證）
  < D02_noreg（6268，意外，機制未明）
    < D03_high（>9000，未完全收斂但持續下滑）
      < D04（從未顯示收斂跡象，目前唯一的「看似真安全」候選）
```

這條排序**不再跟PSR成本或TVLA數字單調對應**（D02_noreg的PSR最高但TVLA判定最差；D04的PSR中等但TVLA最好且抵抗力最強）——這本身是一個值得寫進報告的方法論結論：**沒有任何單一指標（PSR、GE、TVLA）可以獨立預測一個防禦在自適應攻擊下能撐多久，必須三者一起看**，而且即使三者一起看，D02_noreg這種例外仍然可能出現，需要留有餘地、不要把任何排序講死。

### 下一步

- D07的三方比較圖表格已用本節數字更新（原本「未測」的兩格）。
- D02_noreg的異常結果值得追一次：換個種子重跑adaptive攻擊者，確認6268是否穩定重現，這會決定這是真訊號還是訓練雜訊。時間允許的話優先於D08。

## 附錄 M：種子穩健性掃描——D01/D02_noreg的自適應攻擊者結果高度依賴訓練種子，D04是唯一在多種子下保持一致的配方

附錄L結尾提出的追問（D02_noreg的N_TGE=6268是真訊號還是訓練雜訊）促成了這次調查，但結果比原本設想的問題大得多：**不只是D02_noreg的數字不穩定，連D01「幾乎秒破」這個從附錄E就開始引用的核心結論本身，都對訓練種子高度敏感**。

### M.1 方法

用附錄E/L完全相同的流程（`06_generate_defended_trainset.py`產出的`defended_profiling_traces.npy`→攻擊端`01_train_attacker.py --profiling-traces --override seed=X`重訓→`02_run_attack.py --traces`對同一批`defended_traces.npy`（E集）跑推論→`03_evaluate.py`正式100-run評估），唯一變動的是`seed`（原本固定42，這次額外測123跟777）。**split.seed維持42不變**（已用`config_snapshot.yaml`確認A/V/D/E的資料切分兩次完全一致），所以差異只來自權重初始化跟訓練期隨機性，不是資料切分不同造成的混淆。

### M.2 結果：D01三個種子橫跨「秒破」到「幾乎沒被攻破」

| 配方 | seed=42（原始） | seed=123 | seed=777 |
|---|---|---|---|
| **D01** | N_TGE=596，GE@9000=0.00，SR1=1.00（完全崩潰） | N_TGE=None，GE@9000=122.18，SR1=0.00（**幾乎沒被攻破**，貼著隨機基準127.5） | N_TGE=5306，GE@9000=0.00，SR1=1.00（完全崩潰，但比seed=42慢8.9倍） |
| **D02_noreg** | N_TGE=6268，GE@9000=0.04，SR1=0.96（幾乎完全崩潰） | N_TGE=None，GE@9000=103.59，SR1=0.00（僅輕微削弱） | N_TGE=None，GE@9000=33.64，SR1=0.00（明顯削弱但未完全崩潰） |
| D04（附錄H+補測，供對照） | GE@9000=130.37（持平，未見崩潰） | GE@9000=137.68（持平，未見崩潰） | 未測 |

### M.3 對D01的解讀：三個種子裡兩個最終崩潰，但崩潰速度差近9倍；一個完全沒崩潰

**D01不是「穩定秒破」，是「多數情況下最終會被攻破，但何時被攻破、甚至會不會在測試視窗內被攻破，高度依賴訓練隨機性」**。三個種子中，42跟777最終都收斂到GE=0、SR1=1.00的完全崩潰，但N_TGE相差8.9倍（596 vs 5306）；123則在整個9000條軌跡窗口內都沒有實質進展（GE@9000=122.18，只比隨機基準127.5低4%）。

train_history顯示三個種子的最終訓練loss/accuracy彼此非常接近（例如seed=42跟seed=777的loss都在5.16-5.17附近，seed=123則在5.31附近、略高但差距不算大），**但下游的攻擊效果卻天差地遠**——這再次印證附錄A.9/攻擊端CLAUDE.md反覆強調的現象：256類分類的loss/accuracy是攻擊效能的弱代理指標，即使訓練損失看起來相近，模型是否學到「正確方向」的洩漏特徵仍可能有質的差異，而這個差異目前只能透過下游的GE/N_TGE才能偵測到，訓練期的loss本身看不出來。

**附錄E原本引用的「N_TGE=596，只比無防禦時的475多25%」這個具體數字，現在必須加註：這是三個測過的種子裡最快的一個，不能代表D01自適應攻擊者情境下的典型/穩定結果。** 更誠實的表述是：「在測過的3個種子中，2個最終完全崩潰（596、5306），1個在9000條軌跡內未見實質進展；這代表D01對自適應攻擊者的抵抗力不穩定，多數情況下最終會被攻破，但攻破所需的軌跡數可以相差一個數量級以上，也存在攻擊者訓練失敗、防禦意外守住的可能」。

### M.4 對D02_noreg的解讀：附錄L的「10.5倍」本身也不是穩定訊號，但三個種子呈現的仍是「比D01更抗打」的一致方向

D02_noreg三個種子的GE@9000分別是0.04（幾乎完全崩潰）、103.59（幾乎沒被攻破）、33.64（明顯削弱但未完全崩潰）——**同樣呈現高變異，附錄L把seed=42的N_TGE=6268單獨拿出來描述成「比D01晚10.5倍」是一個基於單點的過度精確表述，現在看來不能成立**。

但如果把三個種子放在一起比較兩個配方的**整體傾向**（而不是挑單一個種子的單一數字），一個較弱但仍然一致的訊號浮現：D02_noreg三個種子的GE@9000（0.04、103.59、33.64）平均而言比D01三個種子的GE@9000（0.00、122.18、0.00）**更常落在「有實質但不完全的抵抗」這個中間地帶**（D01傾向兩極：不是完全崩潰就是幾乎沒事，D02_noreg有一個種子清楚卡在中間的33.64）。這個觀察比附錄L的「10.5倍」溫和很多，也還沒有足夠的重複次數（3個種子）能做嚴謹的統計檢定，只能誠實地說「方向上可能存在、但目前的證據強度不足以下定論」。

### M.5 對D04的意義：兩個種子都沒有任何崩潰跡象，是目前唯一通過這一關的配方

**這是本次調查對整個專案最重要的正面確認**：在同樣「換種子看結果會不會大幅擺動」的壓力測試下，D01跟D02_noreg都出現了從「完全崩潰」到「幾乎沒事」的巨大擺動，**D04在兩個測過的種子下（130.37、137.68）都穩定持平在隨機基準附近，沒有任何一個種子顯示出被攻破的跡象**。雖然只測了2個種子（比D01/D02_noreg的3個少一次），但這個一致性本身就是有意義的信號——如果D04的「守住」也是訓練隨機性的產物，理應在至少一個種子上看到明顯的GE下降，但目前沒有觀察到。

**建議報告措辭**：不要再用「D04在自適應攻擊者下未被攻破」這種聽起來像絕對保證的說法，改用「D04在測過的2個獨立訓練種子下，均未觀察到任何被攻破的跡象，相對於D01/D02_noreg在同樣測試下呈現的巨大種子間變異，這個一致性本身是D04優於其他配方的額外證據」——這個表述既誠實（承認樣本數有限）又不失力道（種子間一致本身就是統計上有意義的觀察）。

### M.6 對整份報告的方法論意義

這是繼GE方向誤讀（附錄F）、D05數學證明零效果（附錄D）、D02_noreg打破TVLA-vs-抵抗力排序（附錄L）之後，**第四個獨立的「單一評估結果會製造虛假確定感」案例，而且這次打的是自適應攻擊者這一關本身**——原本引入自適應攻擊者測試（附錄E/D06）是為了修正「靜態凍結攻擊者評估過於樂觀」這個問題，但如果自適應攻擊者測試本身只跑一次，**同樣的問題會用不同的形式重演一次**：一次幸運或不幸的訓練，可以讓同一個防禦看起來「幾乎秒破」或「幾乎守住」。

**修正後的完整驗證協定應該是四層，不是三層**：GE（凍結攻擊者）→ TVLA（model-agnostic）→ 自適應攻擊者（單次）→ **自適應攻擊者的多種子重複**。這第四層在本專案目前的資源下只對三個配方（D01、D02_noreg、D04）做過，其餘配方（D05、D03_high）的自適應攻擊者結果目前都只有單一種子，同樣的但書需要加註在報告裡。

### 下一步

- 已用本節數字更新D07圖表跟兩份已發布的artifact（可行性文件、技術報告）。
- 若時間允許，可以考慮給D03_high、D05也各補一個種子，讓「哪些配方的自適應攻擊者結果需要加但書」這件事有更完整的覆蓋；但鑑於10天期限，這是有餘裕才做的項目，不是必要項目。

## 附錄 N：D08時域干擾基準線——第一個通過TVLA的配方，但被攻擊端已驗證過的resync完全戳破

十日分工計畫「C：D08與資料整理」啟動。跟高斯（振幅域）、GAN家族（學習式）不同，D08測試第三種防禦類別：**時域干擾**——不學習、不加振幅噪訊，單純對每條軌跡做一個隨機的時間平移（desync-style jamming），呼應攻擊端資料庫本身desync50/100難以被攻破的現象（攻擊端CLAUDE.md附錄B.29-B.42）。

### N.1 實作

`src/data/preprocess.py::jamming_augment(traces, max_shift, seed)`（攻擊端repo）：對每條軌跡獨立抽一個`[-max_shift, max_shift]`的整數位移，位移後暴露出的邊界補零（不做wraparound，因為真實目標裝置擷取窗不會循環繞接自己過去/未來的取樣點）。跟`gaussian_augment`同樣是無狀態純函式。新增4個測試（可重現性、跨種子確實不同、已知圖案位移＋補零驗證、shape不變），攻擊端`tests/`現在52個測試全過。串接進`05_apply_defense.py --defense jamming --max-shift N`，跟既有的`--defense gaussian`共用同一套PSR/L2/Linf成本量測跟Stage B銜接流程。

### N.2 4點掃描結果：GE非單調，PI災難性崩潰

對凍結E01攻擊者掃`max_shift ∈ {5, 10, 20, 50}`：

| max_shift | PSR mean | GE@9000（凍結E01） | PI | TVLA masked_value\|t\| | TVLA mask_value\|t\| |
|---|---|---|---|---|---|
| 5 | 0.6167 | 66.05 | -19.50 | 25.55（洩漏） | 23.51（洩漏） |
| 10 | 1.0090 | 46.14 | -35.80 | **4.12（PASS）** | 5.61（洩漏） |
| **20** | 1.2942 | 74.14 | -59.31 | **3.29（PASS）** | **3.38（PASS）** |
| 50 | 1.2313 | 193.69 | -66.78 | **3.87（PASS）** | **3.34（PASS）** |

**max_shift≥20是本專案目前唯一在兩個頻道都通過TVLA門檻（\|t\|<4.5）的配方**——比D04（目前最佳，masked_value\|t\|=20.56，仍超門檻4.6倍）更進一步。GE本身呈現非單調關係（66→46→74→193.69），且PI隨max_shift單調崩潰到-66.78（比附錄C.3高斯σ=3.0的-38.66還負很多）——這是「confidently wrong」現象的又一個、也是目前最極端的實例：cnn_light從未在時間錯位的資料上訓練過，任何位移都會讓固定位置的卷積核完全對錯位置，導致模型不是「不知道」而是「非常自信地押錯」。

### N.3 PSR跟其他防禦類別不可直接比較——這是本節的第一個方法論提醒

D08的PSR（0.62–1.29）比高斯（0.009–0.27）跟GAN家族（0.01–0.15）高一個數量級以上——**這不代表時域干擾的「感知成本」真的比較高，是PSR這個定義（逐軌跡L2範數比）系統性地對時域位移不利**：即使只位移幾個取樣點，逐點比對的軌跡內容也會大量不相關，L2範數會被拉得很大，跟振幅擾動（同一批取樣點只是數值上下微調）不是同一種「性質」的成本。**下次做PSR-vs-效果比較圖時，時域干擾類別需要獨立標示，不能跟振幅域防禦擺在同一條軸上直接比高低**，這是本次調查另一個值得寫進報告的方法論提醒。

### N.4 關鍵檢驗：resync能不能戳破這個TVLA-pass？——結果是能，而且完全戳破

TVLA只在「攻擊者不知道防禦機制、直接拿原始軌跡做統計檢定」這個假設下成立。但攻擊端repo自己（附錄B.30-B.33）已經證實：**對desync50/100這種時間錯位的資料，用盲互相關對齊（`resync`）幾乎能完全恢復SNR**（0.07→6.8+）。如果攻擊者知道防禦是時域干擾（本研究的威脅模型本來就假設攻擊者知道防禦類型並會相應調整，見第三章3.1），沒有理由不套用這個攻擊端已經驗證過的標準對策。

新增`scripts/07_check_resync_bypass.py`（跨repo載入攻擊端`src/data/resync.py`，比照附錄A.11/L的cross-repo模組載入寫法避開`src`套件命名衝突）：用凍結攻擊者的A集（desync0天然對齊）平均軌跡當參考模板，對D08防禦後的E集做`resync`重新對齊，再送回Stage B介面正式評估。

**結果，以TVLA兩頻道都通過的max_shift=20為例**：

```
resync前（naive，無對策）：GE@9000=74.14，N_TGE=None，TVLA masked=3.29(PASS)/mask=3.38(PASS)
resync後（知情攻擊者對策）：GE@9000=0.0000，N_TGE=3811，SR1@9000=1.0000（完全攻破）
              TVLA masked=71.47(LEAKAGE)/mask=53.54(LEAKAGE)——幾乎精確回到clean基準(71.73/52.82)、同一個時間點(517/149)
```

**GE跟TVLA同時、精確地被推回clean基準附近**——resync不是部分削弱這個防禦，是完全、乾淨地逆轉了它。max_shift=50（搜尋範圍同步放大到±50）resync後GE@9000從193.69降到10.23，明顯削弱但未完全攻破（N_TGE仍是None）——這跟攻擊端附錄B.32/B.33觀察到的現象一致：resync的對齊品質會隨著錯位範圍變大而下降（desync100本身的對齊比desync50難），max_shift=50還沒有像max_shift=20那樣被resync乾淨地完全破解，但攻擊效能已經大幅回升，不能再算是有效防禦。

### N.5 對整份報告的意義：第三種、也是最乾淨的一種「TVLA也可能是假象」示範

附錄D（D05）示範了「GE可以是假象、TVLA不會被騙」；本節示範了**「TVLA本身在某些防禦機制下，若不搭配對應的資訊性對策知識，同樣可能被騙」**——TVLA作為model-agnostic的統計檢定，隱含假設「同一個時間點座標，在不同軌跡間代表相同的物理事件」，時域干擾直接攻擊這個假設本身，讓naive TVLA暫時失明，但這個失明可以被一個已知、便宜、攻擊端自己驗證過的標準對策（resync）完全逆轉。**沒有任何單一評估層本身是萬無一失的，四層驗證協定（附錄M）在這裡還需要再加一個提醒：TVLA這一層本身也要考慮攻擊者是否知道並能反制防禦的作用機制，不能只套用naive版本就下結論。**

### 下一步

- D08目前的結論是負面（跟D01/D02/D05同一類：naive評估過關，情境化的知情攻擊者/對策一套上就被戳破），但這個負面結果本身的推導過程（PSR不可比、TVLA假象的第三種機制、resync逆轉的完整量化）是本專案報告方法論章節很好的第三個佐證案例。
- 若時間允許：可以測試「jamming+GAN loss合併訓練」是否能同時規避resync對齊（例如讓產生器的位移本身依賴軌跡內容、動態變化，比固定範圍的均勻隨機位移更難用單一參考模板對齊）——但這已經超出D08「非學習式基準線」的原始定位，屬於全新的研究方向，不在10天範圍內開展，留待未來工作。

## 附錄 O：D04攻擊架構廣度驗證——換成resnet，靜態跟自適應攻擊者都沒有攻破跡象

十日分工計畫「B：廣度與對照」，驗證D04的防護力是否只對`cnn_light`這個特定架構有效，還是能遷移到不同架構的攻擊者。選用`resnet`（3個殘差區塊+GAP，28,816參數，攻擊端repo附錄B.45-B.49已經調過完整4維度超參數：`max_lr=5e-5, end_percentage=0.1, scale_percentage=0.1, epochs=100`）而非`cnn_best`——因為`cnn_best`在攻擊端repo的調查中始終沒有訓練成功（附錄B.44/B.50，5次系統性嘗試全部卡在隨機基準附近），拿一個從未學到東西的攻擊者測試防禦「有沒有被攻破」沒有意義；`resnet`則有一個已知、可重現、真正學到東西的基準（GE@9000=51.32）。

### O.1 重新訓練凍結resnet攻擊者，驗證可重現

用攻擊端repo已驗證的超參數重新跑一次`E07_resnet`：GE@9000=**51.35**，跟附錄B.49記錄的51.32幾乎精確吻合（差異在正常訓練隨機性範圍內），確認這個基準可重現，可以放心當作D04架構廣度測試的攻擊者。

### O.2 靜態（A0）測試：resnet對D04防禦後軌跡的攻擊效能明顯下降

拿這個凍結resnet攻擊D04防禦後的E集（跟cnn_light版E01用的同一批`defended_traces.npy`）：

```
resnet 對乾淨E集（基準）：       GE@9000=51.35
resnet 對D04防禦後E集（A0）：   GE@9000=93.22
```

GE從51.35上升到93.22，代表D04防禦對resnet這個從未見過的攻擊架構同樣造成了實質的攻擊難度提升，不是只對cnn_light的決策邊界起作用。

### O.3 自適應（A1）測試：resnet重訓練後依然沒有攻破跡象

用攻擊端`01_train_attacker.py --profiling-traces`對D04的`defended_profiling_traces.npy`重訓練一個全新的resnet攻擊者（同一套已驗證超參數），評估對D04防禦後E集的攻擊效能：

```
N_TGE  = None（未收斂）
N_SR90 = None
GE @ N=9000  = 74.83
SR1 @ N=9000 = 0.0000
```

**跟cnn_light上觀察到的模式一致**——GE遠低於隨機基準（127.5）但沒有任何收斂到0的跡象，跟D01在resnet上會不會呈現類似附錄M的種子敏感度崩潰模式待驗證（本次只測了單一種子，同樣的但書適用：這是一個資料點，不是統計上有把握的結論，尤其附錄M才剛證明同一件事在cnn_light+D01上就有種子敏感度）。

### O.4 解讀：方向支持D04可遷移，但不是決定性證據

三個數字（51.35→93.22→74.83）呈現的模式跟cnn_light上看到的（D04讓攻擊者無論凍結或重訓練都卡在遠低於隨機基準、但未真正收斂的區間）方向一致，**這是支持「D04的防護機制不是只綁定cnn_light架構」的正面證據**。但必須誠實列出限制：

1. **只測了一個種子**——附錄M剛證明D01在cnn_light上的自適應攻擊者結果對種子高度敏感，resnet上是否也有同樣的敏感度完全未知，這裡沒有時間驗證。
2. **resnet本身的攻擊能力較弱**（基準GE=51.35，遠不如cnn_light的0.00全破），這代表resnet「沒有攻破D04」有一部分可能只是resnet本身攻擊力不足，不完全是D04防禦力強——要更嚴謹地做這個比較，應該用「resnet攻擊乾淨資料 vs resnet攻擊D04防禦後資料」的相對差距（51.35→93.22→74.83，確實都在惡化），而不是拿resnet的絕對GE數字跟cnn_light互相比較。
3. **只測了D04一個配方**，沒有時間對D01/D05等其他配方也做同樣的架構遷移測試，無法排除「resnet對所有配方都一樣難攻破」這個可能性（如果是這樣，前面的解讀就要打折扣）。

**建議報告措辭**：「初步證據顯示D04的防護效果能部分遷移到resnet架構的攻擊者（無論靜態或自適應攻擊者，GE都維持在遠低於隨機基準但未收斂的區間），但這只是單一種子、單一防禦配方的觀察，不足以下定論，需要標註為未來工作的優先驗證項目」——不要說「已證實D04架構無關」，這句話目前的證據支撐不起來。

### 下一步

- 若時間允許，對resnet也補一個第二種子，用同樣的邏輯排除附錄M發現的種子敏感度問題。
- 若時間允許，用同一個resnet攻擊者測試D01，看D01在resnet上是否也一樣容易被攻破（如果是，代表D01的脆弱性也不是cnn_light特有的；如果resnet反而攻不破D01，則說明攻擊架構本身的強弱會系統性影響這整套比較，需要更小心解讀）。

## 附錄 P：補齊D03_high/D05的種子覆蓋——同一個高變異模式再次出現，四個配方（D01/D02_noreg/D03_high/D05）的種子敏感度現在全部有完整3種子記錄

附錄M只對D01、D02_noreg補了seed=123/777；D03_high、D05當時仍只有單一種子（附錄J、L），本節補齊，讓四個做過自適應攻擊者測試的配方都有完整3種子覆蓋（D04仍是2種子，見附錄O的下一步）。

### P.1 結果

| 配方 | seed=42（原始） | seed=123 | seed=777 |
|---|---|---|---|
| D03_high | N_TGE=None，曲線83.63→17.99持續下滑（未完全收斂） | GE@9000=43.09 | GE@9000=132.62（幾乎未被攻破） |
| D05 | N_TGE=597（完全崩潰） | GE@9000=219.60（幾乎未被攻破，高於隨機基準） | GE@9000=7.17（接近完全崩潰但未持續穿越GE=1） |

### P.2 解讀：跟D01/D02_noreg同一個模式——高變異，不是穩定結論

**D03_high**：三個種子分別是「持續下滑未收斂」、「43.09（中等程度削弱）」、「132.62（幾乎沒事）」——同樣呈現從「明顯受影響」到「幾乎沒被攻破」的寬廣分佈，附錄J原本單一種子觀察到的「持續瓦解中」不能當作這個配方的穩定描述。

**D05**：三個種子分別是597（完全崩潰）、219.60（幾乎沒被攻破，甚至比隨機基準127.5還高）、7.17（非常接近完全崩潰但技術上N_TGE仍是None）——**變異幅度不輸給D01**（D01是596/122.18/5306，D05是597/219.60/7.17），附錄L原本引用的「D05完全符合預測、跟D01一樣秒破」這個判斷，現在看來只是三個種子裡剛好抽到最戲劇性的那個，不是穩定行為。

### P.3 四個配方的種子敏感度總表

| 配方 | 靜態TVLA判定 | 3種子GE@9000（或等效指標）分佈 | 變異程度 |
|---|---|---|---|
| D01 | 幾乎假象 | 0.00／122.18／0.00（N_TGE 596/None/5306） | 高（兩極化：破或不破，速度差8.9倍） |
| D02_noreg | 幾乎假象 | 0.04／103.59／33.64 | 高（介於全破跟輕微削弱之間） |
| D03_high | 部分真實 | 未收斂持續下滑／43.09／132.62 | 高 |
| D05 | 數學證明0%效果 | 0.00／219.60／7.17（N_TGE 597/None/None） | 高（兩極化，幅度跟D01相當） |
| **D04（對照）** | **目前最佳** | **130.37／137.68（僅2種子，皆持平）** | **低（唯一穩定的配方）** |

**四個測過3種子的配方全部呈現高變異，只有D04（2種子）保持一致**——這進一步鞏固附錄M的核心結論：**自適應攻擊者測試若只跑一次，不管是哪個配方，都有相當高的機率抽到不具代表性的結果**；而D04在僅有的2次獨立測試中都沒有崩潰跡象，相對而言更加難得，是目前為止對「D04可能真的不一樣」最強的統計佐證（雖然2個種子仍不足以做嚴謹的統計檢定，但在其餘4個配方3種子全部高變異的對照下，D04的2/2一致已經是相對顯著的差異）。

### P.4 對報告的意義

第四章4.7節、第五章5.4節現在可以引用完整的四配方種子敏感度總表，不必再只用D01/D02_noreg兩個例子。建議報告措辭統一為：「除D04外，所有測過多種子的配方（D01/D02_noreg/D03_high/D05）都呈現無法用單一種子代表的高變異，D04是唯一的例外」——這比原本「D01跟D02_noreg不穩定」的敘述更完整，也讓D04的相對優勢更有統計說服力。

### 下一步

- 十日分工計畫「項目1：補齊自適應攻擊者多種子覆蓋」在此完成。
- ~~若時間允許，可以考慮給D04補第三個種子~~ → 已補上，結果見附錄Q——**推翻了本節P.3/P.4「D04是唯一穩定配方」的結論，需要訂正**。

## 附錄 Q：訂正——D04的第三個種子打破了「唯一穩定配方」的敘事，需要更誠實的措辭

附錄P結尾提議給D04補第三個種子「讓統計說服力更強」，抱著會延續前兩個種子（130.37、137.68，皆持平）的預期補跑。**結果沒有延續，反而打破了P.3表格裡「D04是唯一穩定配方」這個才剛寫下的結論**——這是本次調查裡第二次「原本以為在收尾確認，結果推翻了自己剛寫的結論」（第一次是附錄M發現D01不是穩定秒破），必須立刻誠實訂正，不能放著P.3/P.4的舊表述不管。

### Q.1 結果

```
seed=42：  GE@9000=130.37（持平，附錄H）
seed=123： GE@9000=137.68（持平，附錄M）
seed=777： GE@9000=49.31（明顯偏低，N_TGE仍是None，但遠低於前兩個種子）
```

D04三個種子的GE@9000（130.37／137.68／49.31）**不是附錄P.3表格寫的「皆持平」**——seed=777明顯偏離，跟前兩個種子的差距（約80-90）已經不能用雜訊帶過。

### Q.2 修正後的正確框架：D04不是零變異，但是「從未完全崩潰」這件事仍然成立且仍然是唯一的

**必須拿掉的說法**：「D04在測過的種子下都沒有任何崩潰跡象、完全持平」——這句話用兩個種子時看起來成立，用三個種子後不成立，不能再用。

**仍然成立、且仍是D04相對優勢的說法**：把五個配方的「三個種子裡最差（最接近被攻破）的那個結果」放在一起比較：

| 配方 | 三種子GE@9000 | 最差（最接近被攻破）結果 | 是否曾完全崩潰（N_TGE有限值） |
|---|---|---|---|
| D01 | 0.00／122.18／0.00 | **0.00**（完全崩潰） | 是（2/3種子） |
| D02_noreg | 0.04／103.59／33.64 | **0.04**（幾乎完全崩潰） | 是（1/3種子） |
| D03_high | 未收斂下滑／43.09／132.62 | **17.99**（seed42曲線終點，未完全收斂） | 否，但持續下滑中 |
| D05 | 0.00／219.60／7.17 | **0.00**（完全崩潰） | 是（1/3種子） |
| **D04** | 130.37／137.68／**49.31** | **49.31**（明顯削弱，但未完全崩潰） | **否（3/3種子皆未崩潰）** |

**修正後仍然成立的結論**：D04是目前唯一「三個獨立訓練種子裡，沒有任何一個讓GE完全崩潰到0（N_TGE收斂）」的配方——其餘四個配方全部至少有一個種子完全崩潰或幾乎完全崩潰（GE≤0.04）。這個「從未完全崩潰」的質性差異依然存在，且依然是本研究對D04最有力的正面證據。**但「完全沒有崩潰跡象、持平無波動」這個更強的量化說法必須撤回**——D04的seed=777顯示，即使沒有完全崩潰，攻擊效能仍可能受到訓練隨機性顯著影響，只是幅度還沒有大到讓GE穿越1這個門檻。

### Q.3 對報告措辭的具體修正指引

第四章4.5、4.7節與第五章5.4節、第六章6.1節裡所有「D04兩個種子皆持平、無崩潰跡象」的表述，需要改為：**「D04三個獨立訓練種子的GE@9000分別為130.37、137.68、49.31——雖然存在明顯的種子間變異，但三者皆未出現GE收斂至0的完全崩潰，是五個測過多種子的配方中唯一沒有任何種子完全崩潰的候選」**。附錄P.4建議的報告措辭（「D04是唯一的例外」）需要降級為「D04是唯一沒有完全崩潰過的候選，但並非完全免疫於種子變異」，避免過度宣稱。

### Q.4 這次訂正本身的方法論意義

這是本次調查第三次在準備收尾/寫報告階段發現自己前一步的結論被推翻（第一次：附錄F的GE方向；第二次：附錄M的D01種子敏感度）。**每一次都印證同一件事：在時間壓力下，越接近報告截止日期，越容易把「目前手上兩三個數據點看起來一致」直接寫成「已證實」，而這正是本研究整份報告最核心的方法論警告**（見第五章）。這次的訂正沒有花太多額外成本（一次訓練+評估，約10分鐘），代價是必須回頭修改CLAUDE.md、docs/runs.md、README、report_draft.md與兩份已發布的artifact裡所有引用到「D04皆持平」的段落——這個修改成本本身也是一個論點：**及早、便宜地驗證比事後大範圍訂正更划算**，報告定稿前應該把這類「僅2個數據點就下結論」的段落全部再檢查一次。

### 下一步

- 已同步修正docs/runs.md、README.md、docs/report_draft.md（第四、五、六章）與兩份已發布的artifact（可行性文件、D07圖表）。
- 十日分工計畫「項目1：補齊自適應攻擊者多種子覆蓋」現在真正完成，五個配方全部達到3種子覆蓋，且結論已根據完整資料訂正。

## 附錄 R：resnet補第二個種子——結果方向反轉，附錄O「初步支持可遷移」的結論需要降級為「不確定」

附錄O當時明確寫下限制：「只測了單一種子...建議報告措辭是『初步證據支持可遷移』」，並在下一步建議「若時間允許，對resnet也補一個第二種子」。這裡補上，結果不是強化附錄O的結論，而是推翻了它的方向。

### R.1 結果

| 種子 | resnet乾淨基準GE | 攻擊D04防禦後E集（凍結A0） | 攻擊D04防禦後E集（重訓練A1） |
|---|---|---|---|
| **42**（附錄O原始） | 51.35 | 93.22（**↑**，防禦讓攻擊變難） | 74.83 |
| **123**（本次補測） | 44.35 | **30.52（↓，攻擊反而變容易）** | **35.99（↓，同樣變容易）** |

**seed=42顯示D04讓resnet攻擊變難（GE從51.35升到93.22），seed=123卻顯示相反方向（GE從44.35降到30.52）**——不是「效果比較弱」，是**方向直接反過來**，凍結跟重訓練兩種情境都是同一個反轉模式，不是單一次量測的偶然。

### R.2 解讀：附錄O的「初步支持可遷移」結論不成立，需要降級為「不確定，方向在兩個種子間反轉」

**必須拿掉的說法**：附錄O寫的「三個數字呈現的模式跟cnn_light上一致...是D04防護機制不限於cnn_light架構的正面證據」——這句話在只有一個種子時看似合理，加入第二個種子後方向直接反過來，不能再這樣寫。

**目前只能誠實承認的狀態**：D04對resnet攻擊者的效果在兩個測過的種子之間**方向不一致**——一個顯示防禦有效（讓攻擊變難），另一個顯示防禦看似沒有保護效果甚至讓攻擊變容易。這跟cnn_light上的情況本質不同：cnn_light上D04三個種子（130.37/137.68/49.31）雖然幅度不同，但**方向一致**——D04防禦後的資料，三次重訓練攻擊都遠低於隨機基準、且沒有一次完全崩潰，資料本身確實比較難學。resnet上出現的方向反轉，暗示防禦對resnet這個架構的效果可能本身就不穩定，甚至可能根本沒有跨架構遷移——兩個種子還不足以判斷是「resnet需要更多種子才會顯現一致方向」還是「D04對resnet真的沒有穩定效果」。

**可能的解釋（皆未驗證，並列記錄不做取捨）**：
1. resnet本身的攻擊力偏弱（兩個種子的乾淨基準GE本身就有51.35/44.35的14%差異），在這麼弱的訊噪比下，「乾淨 vs 防禦後」的差異可能大部分是resnet訓練本身的雜訊，不是D04真正發揮的效果——這個解讀如果成立，代表D04的效果可能從一開始就沒有真的遷移到resnet，附錄O第一次看到「變難」只是雜訊剛好朝防禦有利的方向。
2. resnet在不同種子下學到的洩漏特徵本質不同（不像cnn_light固定學同一組POI），D04針對cnn_light已知洩漏點設計的loss，可能對resnet某些訓練結果有效、對另一些完全無效，取決於resnet這次剛好學到哪些特徵——這個解讀如果成立，代表「D04能不能遷移到resnet」這個問題本身沒有單一答案，要看resnet訓練出來的具體模型。

### R.3 對報告的意義

第四章4.8節、第六章6.1/6.2節裡所有「初步證據支持D04可遷移到resnet」的表述都需要下修。建議統一改為：**「D04對resnet攻擊者的效果在兩個測過的種子間方向不一致（一個顯示防禦有效、一個顯示無效甚至反效果），不足以支持或否定『D04可遷移至異架構』這個假說，需要更多種子（建議至少5個，且應涵蓋多個獨立訓練的resnet基準模型）才可能得出可信結論，這是本研究攻擊架構廣度驗證中最不確定的一環」**。

這是本研究第四次「補測本來想強化某個結論，結果推翻了它」的案例（前三次：GE方向、D01種子敏感度、D04第三種子）。跟前三次不同的是，這次連「哪個方向才是對的」都還不清楚——不是「原本的正面結論被推翻成負面」，是「連正面/負面本身都無法判斷」，這比前三次的訂正更根本地凸顯了「攻擊架構廣度」這個維度本身取樣不足的問題（目前只有2個resnet訓練樣本，遠不如cnn_light的3個）。

### 下一步

- 若時間允許，resnet應該至少再補2-3個種子（總共4-5個），才有機會判斷方向反轉是雜訊還是真訊號；在那之前，「D04是否能遷移到resnet」這個問題應該誠實列為未解，不寫進正式結論。
- 這也連帶提醒：cnn_light上的D04結論（3種子皆未完全崩潰）目前看來相對穩固，但也只有3個樣本，report應避免把resnet的不確定性過度連結到cnn_light的結論上——兩者是獨立的證據鏈，不應該因為其中一個不穩定就懷疑另一個。
